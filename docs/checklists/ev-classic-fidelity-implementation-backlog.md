# EV Classic fidelity implementation backlog

Purpose: live checklist for recommendations and potential Terminal Velocity implementations that come out of original EV Classic observation, decoded resources, or comparison work.

Use contract: this is an execution surface, not a passive idea dump. New evidence should either produce a small source-backed implementation slice, a labeled Terminal Velocity scaffold, or a bounded `needs evidence` / `blocked` next action. Implemented slices stay here only as regression/provenance anchors until they are summarized elsewhere; if this file stops naming concrete next actions, it is stale and should be groomed before adding more notes.

Anti-staleness rule: before choosing fresh work, prefer an existing `ready`, `implemented / needs Classic confirmation`, or narrow `needs evidence` item whose next action is safe local work. If an item has only broad rationale or a long implementation history, compact it into a short current state plus next action and move detailed rationale to the linked research/decision artifact.

Rationale/source record: `docs/decisions/2026-05-19-ev-classic-observation-to-implementation-workflow.md`.

Status vocabulary: `candidate`, `needs evidence`, `ready`, `implemented`, `verified`, `deferred`, `blocked`.

## Active / recurring workflow

- [x] Maintain observation-to-implementation split
  - Status: `verified`
  - Source: user request on 2026-05-19 plus workflow artifact above.
  - Rule: every durable recommendation from observation work should either be implemented immediately if small/source-backed, or captured here with a next action.
  - Storage rule: original-system observations that may inform future Terminal Velocity behavior, data, UI, economy, services, missions, combat, hazards, or progression must be recorded in `docs/research/original-ev-classic-runtime-observations.md` with evidence/provenance before implementation or deferral decisions are made.

- [x] Seed playable observations for later Terminal Velocity incorporation
  - Status: `verified`
  - Source: user clarification on 2026-05-20: as EV Classic is played, observations should be seeded into artifacts so they can later be incorporated into Terminal Velocity.
  - Rule: every play session should produce zero or more observation seeds in `docs/research/original-ev-classic-runtime-observations.md` using the seed format there. A seed is not automatically implementation authority; it is a durable handoff containing the observed behavior, learned player skill/use, evidence, likely Terminal Velocity surface, and missing evidence/status.
  - Next action during play: after each meaningful original-runtime observation, add or update a seed, then either link it to an existing backlog item or create a compact candidate/needs-evidence item here.

- [x] Reuse learned EV player skill sets in Terminal Velocity design
  - Status: `verified`
  - Source: user guidance on 2026-05-20: the skill set learned while playing EV Classic can later be utilized for Terminal Velocity.
  - Rule: treat learned player skills as reusable design material, not just operator notes. Capture mission-running, trading, route planning, fuel/risk management, combat avoidance/engagement, ship modification, outfitting, and system-provisioning judgment in observation seeds when they are learned from play. Mark whether each learned skill can transfer to other pilots/test profiles, and distinguish transferable player strategy from pilot-specific state, cargo, reputation, mission deadlines, equipment, or save-risk constraints.
  - Future incorporation surfaces: Terminal Velocity tutorials/onboarding, hints, mission/economy balance, AI/player guidance, progression design, regression scenarios, and test pilots.

- [x] Report mission completion and pirate/hostile avoidance during play
  - Status: `verified`
  - Source: user guidance on 2026-05-20 requesting reports about how missions are completed and how pirates or other threats are avoided.
  - Rule: mission/travel play reports should document the active mission, route selection, cargo/passenger constraints, fuel/credit changes, completion/failure state, encountered pirates/hostiles/asteroids, avoidance or engagement tactics, damage/risk outcome, and reusable lessons for other pilots.
  - Future incorporation surfaces: Terminal Velocity mission tutorials, route-risk hints, pirate/hostile threat modeling, defensive-readiness balance, AI guidance, and regression scenarios for safe travel.

- [x] Review Classic quirks separately from bugs before adopting them
  - Status: `verified workflow rule`
  - Source: user guidance on 2026-06-04: mulvray wants to review discovered quirks and decide whether they are bugs.
  - Rule: newly discovered odd/surprising/exploitable/inconsistent/later-fixed Classic behavior must be logged in `docs/research/ev-classic-quirk-review-ledger.md` with evidence, candidate classification, why-it-might-be-a-quirk, why-it-might-be-a-bug, recommended Terminal Velocity handling, and `User decision: pending` before it becomes an intentional fidelity target.
  - Default handling: preserve intentional mechanics and player-legible quirks; fix destructive/inconsistent/platform-artifact bugs; treat later EV-family fixes as review evidence, not automatic authority. Harmful, invisible, crashy, save-corrupting, misleading, implementation-artifact-only, or platform/emulator-specific defects should become clean Terminal Velocity behavior unless user review classifies them as part of the compatibility contract. Plug-in/data compatibility defects should be modeled as optional compatibility paths or migration shims before they become default runtime behavior.

## Source-backed implementations already integrated

- [x] EV Classic starting credits
  - Status: `verified`
  - Evidence: `original-runtime-observed`; first playable HUD shows `Credits: 10,000`.
  - Implementation: `godot_ev/scripts/main.gd` uses `credits := 10000`.
  - Verification: Python model tests and Godot selftest passed in the 2026-05-19 observation pass.

- [x] EV Classic start at Levo in space
  - Status: `verified`
  - Evidence: `original-runtime-observed` plus `decoded-resource-backed` Levo landing text.
  - Implementation: `native_ev/data/universe.json` starts with Levo; `godot_ev/scripts/main.gd` resolves `START_SYSTEM_NAME := "Levo"` by name.
  - Caveat: Levo routing/coordinates are minimal bridge scaffolding pending fuller topology work.

- [x] Levo landing panel visible services
  - Status: `verified`
  - Evidence: `original-runtime-observed`; Levo landed screen exposes `Spaceport Bar`, `Mission Computer`, `Commodity Exchange`, and `Leave`, with no visible outfitter button.
  - Implementation: Levo inventory/services omit outfitter/weapons listings; Godot landing panel only shows outfitter/shipyard when listings exist.

- [x] Levo commodity names, statuses, buy prices, and same-port sell prices
  - Status: `verified`
  - Evidence: `original-runtime-observed`; Levo Commodity Exchange visible rows and buy/sell-back captures.
  - Implementation: `native_ev/data/economy.json` uses Food `120`, Industrial `192`, Medical `600`, Metal `144`, Equipment `360` for both buy and sell at Levo, with EV Classic status labels where observed.
  - Caveat: this proves Levo same-port sell prices, not the global economy formula.

## Development method / process optimization

- [x] Source-aligned vertical-slice development method
  - Status: `verified`
  - Source/rationale: `docs/research/source-aligned-game-development-method.md`; uses project-local automation synthesis plus Godot best-practice docs and practitioner game-development sources on vertical slices, backlog granularity, agile/demonstrable iterations, and playtesting.
  - Rule: develop gameplay as small vertical slices with one player-visible or symbolic behavior, one named scenario/evaluator, one cheap verification command, one source/fidelity label, and a backlog/docs update when future behavior is affected.
  - Kanban rule: use direct TDD inside a tight single-slice loop; use Kanban only at feature/lane boundaries such as Godot UI, symbolic model, original EV observation, source/fidelity docs, and review.
  - Fidelity guardrail: general game-development and automation sources improve method only; they do not justify EV Classic behavior claims.

- [x] Mine exposed original-game variables before treating manuals as complete authority
  - Status: `verified workflow rule`
  - Source: user correction on 2026-05-29.
  - Rule: when extracting EV/EV-family mechanics, mine exposed variables/fields from the original games/resources in addition to the EV Bible and plug-in manuals. Record decoded original-game variables separately from manual/Bible guidance and runtime observations so implementation labels can distinguish `decoded-original-variable`, `manual-or-bible-guided`, `runtime-observed`, and `Terminal Velocity scaffold` evidence.
  - Next action: future combat, mission, government/legal, ship/outfit, economy, and AI slices should first inspect the relevant decoded original records/manifests or add extractor coverage where fields are not yet exposed, then use manuals/Bibles to name and interpret fields rather than as the only data source.

## Candidates / potential implementations

- [ ] Player strategy skills as distinct progression loops
  - Status: `candidate`
  - Source: user framing on 2026-05-20; partially supported by observed Mission Computer, Spaceport Bar, Commodity Exchange, cargo, credits, passenger-mission UI, and later user guidance to learn opportunistic trade-with-missions and ship modification as separate skills.
  - Scope: model separate-but-overlapping skills/strategies for mission running, commodity trading, piracy/privateering/looting, faction progression, and upgrade/ship-ladder planning instead of treating credits as a single undifferentiated grind.
  - Design direction: players should be able to develop skill/capability in various niches, with different pilots specializing in different loops rather than every pilot converging on the same optimal path. Mission runners should learn when spare cargo capacity, route alignment, capital, deadlines, and safety make it worth carrying trade goods alongside jobs; ship modification and ship trade-up should be learned as capability-planning loops, including cargo/storage capacity, hardening, instruments/equipment, and when weapons/outfits/escorts are needed for defensive survival versus when avoidance/speed/route choice is enough. Combat-adjacent learning should include system-level hazards such as asteroids, pirate/hostile traffic levels, when to avoid other ships, and when attacking is worthwhile or too risky, but destructive/legal-consequence tests stay on disposable non-strict pilots. As exploration reaches new systems or landable bodies, verify whether Terminal Velocity needs original-game planet graphics and stores/services provisioned there, including commodities, outfitters, weapon availability, shipyards, and absent services.
  - Plan: `docs/plans/2026-05-20-player-strategy-niches.md`.
  - Implementation scaffold: symbolic scenario `mission_trade_hybrid_capacity_planning` now exercises accepting an 8-ton courier job, buying one 10-ton commodity lot with the remaining hold, blocking a second lot for insufficient capacity, completing the mission, and retaining the trade cargo as a Terminal Velocity mission/trade hybrid capacity-planning scaffold. Symbolic scenario `mission_trade_refuel_delivery_loop` extends the hybrid loop from Earth: accept the intro courier, buy one Sol Food lot in the remaining hold, block the Centauri delivery leg on empty fuel, refuel at Earth, then deliver at Luna while retaining trade cargo under `sourceLabel=terminal-velocity-mission-trade-refuel-scaffold` / `oracleStatus=mission_trade_refuel_pending_classic_runtime_trace`. Symbolic scenario `mission_trade_destination_sale_loop` follows the same mission/trade hybrid through destination-market cleanup: after the Luna delivery, it sells the carried Sol Food lot in Centauri's active Terminal Velocity market, clears the hold, and records `sourceLabel=terminal-velocity-mission-trade-destination-sale-scaffold` / `oracleStatus=mission_trade_destination_sale_pending_classic_runtime_trace`. Symbolic scenario `chapter_one_trade_carryover_loop` carries a Sol Food lot through two noncombat story deliveries, reserves the second mission's cargo alongside the held trade lot, then sells the lot at Sirius Station under `sourceLabel=terminal-velocity-chapter-one-trade-carryover-scaffold` / `oracleStatus=chapter_one_trade_carryover_pending_classic_runtime_trace`. Symbolic scenario `mission_trade_return_margin_guardrail` evaluates an unprofitable Sirius Equipment return cargo candidate for the Sol return leg, records a skip decision (`marginPerTon=-10`) under `sourceLabel=terminal-velocity-mission-trade-return-margin-scaffold` / `oracleStatus=chapter_one_return_trade_margin_pending_classic_runtime_trace`, then completes the third return courier contract without contaminating the hold. Godot F10 help now surfaces that mission/trade hybrid loop as player guidance (`accept cargo jobs first, then buy only commodity lots that fit remaining free hold`) and `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasMissionTradeHybridHelp=true`. Symbolic scenario `outfitter_ship_ladder_intro` exercises the non-destructive Terminal Velocity manifest path for reaching Earth, buying a cargo upgrade and weapon, injecting test-only purchase capital, and trading up from Shuttlecraft to Light Freighter while recording `terminal-velocity-outfitter-ship-ladder-scaffold` source labels and pending-original-trace oracle status. Godot F10 help now separately reminds players to compare cargo, fuel, hull, outfits, and weapon slots before trading up, and the help log verifies `hasShipLadderHelp=true`.
  - Implementation addendum: F10 help now surfaces the verified return-leg margin guardrail: after delivering a contract, compare destination and linked-market prices, skip negative-margin cargo, and finish the contract chain under Terminal Velocity scaffold labels pending Classic evidence.
  - Implementation addendum: symbolic scenario `strategy_skill_rotation_loop` records distinct controller/player strategy checkpoints for `merchant`, `mission_runner`, and `route_planner` in one noncombat loop: travel to Earth, evaluate/carry a profitable Sol Food lot, accept the intro courier, queue the active mission route, deliver at Luna, then sell the carried trade cargo. Events use `sourceLabel=terminal-velocity-strategy-skill-rotation-scaffold` / `oracleStatus=strategy_skill_progression_pending_ev_family_source_trace`; this is Terminal Velocity learning scaffolding, not an EV Classic fidelity claim.
  - Implementation addendum: symbolic scenario `upgrade_readiness_strategy_loop` records upgrade-planning checkpoints for `service_scout`, `outfitter`, `weapons`, and `ship_buyer`: travel to Earth, scan the service matrix, buy a cargo pod, buy a laser cannon, and trade up to a light freighter. Events use `sourceLabel=terminal-velocity-upgrade-readiness-strategy-scaffold` / `oracleStatus=upgrade_strategy_progression_pending_ev_family_source_trace`; this remains Terminal Velocity upgrade-readiness scaffolding pending EV-family source/runtime confirmation.
  - Implementation addendum: symbolic scenario `upgrade_affordability_trade_loop` records a separate upgrade-affordability planning loop: an initial light freighter purchase is blocked by insufficient credits, the pilot runs a profitable two-lot Sol Food -> Levo funding trade, returns to Earth, and buys the light freighter. Events use `sourceLabel=terminal-velocity-upgrade-affordability-strategy-scaffold` / `oracleStatus=upgrade_affordability_progression_pending_ev_family_source_trace`; the near-goal funding pattern remains Terminal Velocity scaffold evidence pending EV-family source/runtime confirmation.
  - Implementation addendum: symbolic scenario `cargo_expansion_trade_loop` records a capacity-upgrade planning loop: the starting shuttle fills its 20-ton hold with two Sol Food lots, blocks a third lot for insufficient cargo space, buys a Cargo Pod at Earth, carries the expanded three-lot load to Levo, and sells all cargo for `11140` final credits. Events use `sourceLabel=terminal-velocity-cargo-expansion-trade-scaffold` / `oracleStatus=cargo_expansion_trade_pending_classic_runtime_trace`; this remains Terminal Velocity scaffold evidence pending original Classic runtime/source confirmation of broader cargo-upgrade strategy.
  - Implementation addendum: symbolic scenario `fuel_reserve_upgrade_loop` records a fuel-reserve upgrade loop: the pilot reaches Earth, surfaces an empty-reserve range gap, buys an Auxiliary Fuel Tank, refuels to the expanded `31`-unit reserve, and returns to Levo with `30` fuel remaining after the hop. Strategy checkpoint events use `sourceLabel=terminal-velocity-fuel-reserve-upgrade-scaffold` / `oracleStatus=fuel_reserve_upgrade_pending_classic_runtime_trace`; this remains Terminal Velocity scaffold evidence pending original Classic runtime/source confirmation of fuel-reserve upgrade strategy and exact reserve semantics.
  - Implementation addendum: symbolic scenario `light_freighter_repair_margin_loop` records a Light Freighter repair-budget recovery loop: a damaged Light Freighter at Earth identifies profitable Sol Food for the Levo run, spends its narrow cash on two lots, sells at Levo to fund a return to Earth, then repairs `260/300` hull to full at the service port. Events use `sourceLabel=terminal-velocity-light-freighter-repair-margin-scaffold` / `oracleStatus=light_freighter_repair_margin_pending_classic_runtime_trace` plus the existing `terminal-velocity-repair-service-scaffold` repair boundary; this is Terminal Velocity scaffold evidence pending Classic runtime/source confirmation of repair-funded freighter strategy.
  - Implementation addendum: symbolic scenario `light_freighter_repair_mission_margin_loop` extends the damaged Light Freighter repair budget lane into a mixed mission/trade run: a `260/300` Light Freighter reserves a 120-ton Levo bulk delivery, fills the remaining 30 tons only with positive-margin Sol Food, completes the delivery, sells retained cargo, returns to Earth, and repairs to full hull. Events use `sourceLabel=terminal-velocity-light-freighter-repair-mission-margin-scaffold` / `oracleStatus=light_freighter_repair_mission_margin_pending_classic_runtime_trace` plus the existing repair-service scaffold boundary; this remains Terminal Velocity scaffold evidence pending Classic runtime/source confirmation of damaged freighter mission/trade/repair ordering.
  - Implementation addendum: symbolic scenario `light_freighter_repair_refuel_mission_margin_loop` adds a low-fuel recovery step to the damaged freighter mixed mission/trade repair lane: a `260/300` Light Freighter reserves the 120-ton delivery, spends its remaining cash on positive-margin Sol Food, reaches Levo on its last fuel unit, completes delivery, sells retained cargo, blocks the empty-fuel return to Earth, refuels at Levo, then returns and repairs to full hull. Events use `sourceLabel=terminal-velocity-light-freighter-repair-refuel-mission-margin-scaffold` / `oracleStatus=light_freighter_repair_refuel_mission_margin_pending_classic_runtime_trace`.
  - Implementation addendum: symbolic scenario `light_freighter_deadline_repair_refuel_margin_loop` adds deadline pressure to the same damaged Light Freighter margin lane without claiming exact Classic UI wording: a `260/300` Light Freighter reserves the 120-ton Earth-to-Levo delivery, fills only the remaining 30 tons with positive-margin Sol Food, reaches Levo on its last fuel unit, advances to the mission's final allowed day, completes before late-failure processing, sells trade cargo, refuels, returns to Earth, and repairs to full hull. Events use `sourceLabel=terminal-velocity-light-freighter-deadline-repair-refuel-margin-scaffold` / `oracleStatus=light_freighter_deadline_repair_refuel_margin_pending_classic_runtime_trace`.
  - Verification addendum: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_light_freighter_deadline_repair_refuel_margin_loop_delivers_before_repair native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_available_scenarios_exposes_curriculum_order`; `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'`; `python3 tools/run_gameplay_scenarios.py --all --pretty`; `./run_godot.sh self-test`.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_trade_refuel_delivery_loop_blocks_low_fuel_then_delivers_with_trade_cargo -v`; `python3 tools/run_gameplay_scenarios.py mission_trade_refuel_delivery_loop --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_trade_destination_sale_loop_sells_cargo_after_delivery -v`; `python3 tools/run_gameplay_scenarios.py mission_trade_destination_sale_loop --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_chapter_one_trade_carryover_loop_sells_cargo_after_second_delivery -v`; `python3 tools/run_gameplay_scenarios.py chapter_one_trade_carryover_loop --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_trade_return_margin_guardrail_skips_bad_return_cargo_and_finishes_chain -v`; `python3 tools/run_gameplay_scenarios.py mission_trade_return_margin_guardrail --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_strategy_skill_rotation_loop_records_distinct_playstyle_checkpoints -v`; `python3 tools/run_gameplay_scenarios.py strategy_skill_rotation_loop --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_upgrade_readiness_strategy_loop_records_service_outfit_weapon_and_ship_checkpoints -v`; `python3 tools/run_gameplay_scenarios.py upgrade_readiness_strategy_loop --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_upgrade_affordability_trade_loop_funds_upgrade_after_initial_credit_gate -v`; `python3 tools/run_gameplay_scenarios.py upgrade_affordability_trade_loop --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_cargo_expansion_trade_loop_uses_cargo_pod_for_third_trade_lot -v`; `python3 tools/run_gameplay_scenarios.py cargo_expansion_trade_loop --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval -v`; `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'`; `python3 tools/run_gameplay_scenarios.py --all --pretty`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_self_test_covers_gameplay_scenario_curriculum -v`; `./run_godot.sh self-test`.
  - Next action: gather original-runtime evidence for each loop using separate non-strict pilots where useful: mission/trade/navigation pilot first, commodity spread pilot second, outfitting/ship-stats pass third, combat/piracy/privateering on a disposable non-strict pilot only after controls and save safety are understood.
  - Pilot policy: creating multiple pilots for different strategy tracks is approved by the user; keep Strict Play off unless explicitly directed otherwise.
  - Do not implement piracy/legal/faction consequences from inference alone; require original runtime, decoded resources, or manual/source evidence.

- [ ] Fuller EV Classic galaxy topology and coordinates
  - Status: `needs evidence`
  - Source: current Levo bridge is intentionally minimal.
  - Next action: decode/source-integrate topology records or capture/runtime-verify map links before broad universe changes.
  - Do not implement from adaptation data alone.

- [x] EV Classic map multi-stop Shift-click route planning
  - Status: `verified scaffold / screenshot-confirmed original route-path example; needs edge-case pass`
  - Source: user-observed original-runtime behavior in Basilisk II on 2026-05-28; user-provided screenshot-confirmed example on 2026-05-29; see `docs/research/original-ev-classic-runtime-observations.md#2026-05-28-map-multi-stop-route-planning-seed`.
  - Scope: holding Shift and clicking multiple available systems should extend a green multi-hop route path. Terminal Velocity now stores an ordered route queue/path, appends only systems linked from the current route tail, draws the full green polyline, clears an explicit queued route with Backspace/Delete, and consumes/advances the first leg on jump.
  - Implementation: Godot map route selection uses `selected_route` with tail-linked append, invalid-click/current-system route preservation, explicit route clearing (`_clear_selected_route`), re-selection after clear, and full green route drawing; symbolic scenarios `shift_click_multi_stop_route_queue`, `route_queue_invalid_stop_guardrail`, `route_queue_clear_guardrail`, and `route_queue_clear_reselect_guardrail` verify Levo → Sol → Sirius queue construction, first-leg consumption, invalid-click preservation, clear-to-no-destination blocking, and fresh route recovery after clear.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval -v`; `python3 tools/run_gameplay_scenarios.py --all --pretty`; `./run_godot.sh tv-map-route-log`; `./run_godot.sh tv-route-invalid-log`; `./run_godot.sh tv-route-clear-log`; `./run_godot.sh tv-route-clear-reselect-log`; `./run_godot.sh tv-route-jump-log`; `./run_godot.sh self-test` passes with `gameplayScenarios=18`.
  - Next action: capture an original-runtime step-by-step Basilisk pass when input/capture is reliable enough to verify exact edge cases such as Classic route clearing input, no-destination behavior after clear, and path truncation.

- [x] EV Classic near-system-center hyperspace block
  - Status: `implemented / original-runtime-observed label with exact distance pending`
  - Source: original-runtime probe found a queued route jump can be blocked while the ship remains too close to the system center; exact EV Classic threshold still needs measurement.
  - Scope: Terminal Velocity now blocks hyperspace before fuel consumption/system transition when `distanceFromSystemCenter` / Godot `pos.length()` is below the current scaffold threshold, preserving current system and fuel while reporting the original-runtime-observed failure text.
  - Implementation: symbolic scenario `near_center_jump_block` records a `blocked_jump` with `reason="too close to system center"`, `sourceLabel=original-runtime-observed`, preserved fuel/system checks, and `minJumpDistance`; Godot `_jump()` enforces `MIN_HYPERSPACE_DISTANCE_FROM_CENTER` and exposes `./run_godot.sh tv-near-center-jump-log` plus the Windows `-NearCenterJumpLog` wrapper. The broader `./run_godot.sh tv-navigation-guardrail-log` now also verifies the source-backed too-close jump message alongside no-route, low-fuel, landing-approach, and refuel blocked guidance. F10 help now surfaces a near-center jump recovery line telling players to keep the route selected, fly outward, and retry `J` before changing destination, under the original-runtime-observed/exact-distance-pending boundary.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval`; `python3 tools/run_gameplay_scenarios.py --all`; `./run_godot.sh self-test`; `./run_godot.sh tv-near-center-jump-log`; `./run_godot.sh tv-navigation-guardrail-log`; regression probes `./run_godot.sh tv-route-jump-log` and `./run_godot.sh tv-low-fuel-jump-log`. 2026-06-05 addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v` and `./run_godot.sh tv-gameplay-curriculum-help-log` verify `hasNearCenterJumpRecoveryHelp=true`; `./run_godot.sh tv-near-center-jump-log` still reports `jumpBlocked=true` / `blockReason=too_close_to_system_center`.
  - Next action: measure exact original EV Classic distance/wording if a later Basilisk probe can capture the transition point cleanly.

- [ ] Exact starting primary weapons/outfits
  - Status: `needs evidence / player-facing uncertainty helper and deterministic starting-equipment contract added`
  - Source: first HUD proves `No Secondary Weapon`, `No Target`, full shield/fuel, and `Free: 20`; it does not prove full primary weapon/outfit inventory.
  - Implementation addendum: F10 help now surfaces this evidence boundary as a starting-equipment uncertainty reminder: players can rely on the original-runtime-observed `No Secondary Weapon`, `No Target`, full shield/fuel, and `Free: 20` HUD facts, but should check Player Info or a non-mutating outfitter/status surface before treating primary weapons/outfits as exact Classic data. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasStartingEquipmentUncertaintyHelp=true` under the existing help-surface pending-playtest boundary.
  - Implementation addendum: 2026-06-06 added `./run_godot.sh tv-starting-equipment-log`, `--tv-starting-equipment-log`, and `TV_STARTING_EQUIPMENT_EVENT` to pin the starting-state contract without mutating inventory. The probe reports original-runtime-observed starting HUD facts separately from Terminal Velocity/source-mined primary visibility: `startingSecondaryHudObserved=true`, `startingNoTargetObserved=true`, `startingFuelFullObserved=true`, `startingFreeCargoObserved=true`, `primaryInfoVisible=true`, `primarySourceStockName="Laser Cannon"`, `secondaryInfoVisible=true`, `sourceLabel=original-runtime-observed-starting-hud-plus-terminal-velocity-source-mined-primary`, and `oracleStatus=starting_primary_and_outfits_pending_nonmutating_classic_status_trace`.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_starting_equipment_log_contract -v`; `./run_godot.sh tv-starting-equipment-log`; `python3 -m unittest native_ev.tests.test_model -v` (128 tests).
  - Next action: open a player/ship info screen or an outfitter/status screen in EV Classic at a port that exposes one, without changing inventory, to confirm primary weapons/outfits beyond the starting HUD.

- [x] EV Classic commodity transaction granularity and Levo same-port sellback
  - Status: `implemented / source-backed scenario coverage added`
  - Source: Levo buy/sell observation showed `Buy` purchases 10 tons at a time from the starting shuttle state, and same-port sellback restores starting credits/cargo for observed Levo lots.
  - Implementation: `godot_ev/scripts/main.gd` uses `EV_CLASSIC_COMMODITY_LOT_SIZE := 10` for commodity buy/sell actions, bounded by free cargo, credits, held quantity, and landed state. Symbolic scenario `levo_same_port_sellback_loop` buys one 10-ton Levo Food lot, sells it back at Levo, and verifies credits/cargo return to the starting state with `sourceLabel=original-runtime-observed` / `oracleStatus=levo_same_port_sellback_observed`. Symbolic scenario `commodity_sell_blocked_recovery_loop` now covers sell-side blocker recovery: selling with no held lot and selling while in space both block with `terminal-velocity-trade-scaffold` labels, then landing/selling restores the start trade state. Symbolic scenario `commodity_buy_blocked_recovery_loop` now covers buy-side blocker recovery: buying while in space, with insufficient credits, and with insufficient capacity all block with `terminal-velocity-trade-scaffold` labels, then landing/buying one Levo Food lot recovers to a valid held-cargo state.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_levo_same_port_sellback_loop_restores_credits_and_cargo -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_commodity_sell_blocked_recovery_loop_labels_blockers_and_recovers -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_commodity_buy_blocked_recovery_loop_labels_blockers_and_recovers -v`; `python3 tools/run_gameplay_scenarios.py levo_same_port_sellback_loop --pretty`; `python3 tools/run_gameplay_scenarios.py commodity_sell_blocked_recovery_loop --pretty`; `python3 tools/run_gameplay_scenarios.py commodity_buy_blocked_recovery_loop --pretty`; broader native/Godot verification in the 2026-06-04 sellback/guardrail scenario slices. Godot F10 help now surfaces a commodity blocked-action recovery hint, and `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasCommodityRecoveryHelp=true`.
  - Caveat: full partial-capacity original-runtime behavior and non-Levo economy spreads remain edge-case observation candidates if exact EV behavior matters.

- [ ] System-by-system original service/store provisioning
  - Status: `needs evidence / Terminal Velocity scouting scaffold added`
  - Source: user guidance on 2026-05-20: as other systems are explored, determine whether they need to be provisioned with original-game planet graphics, stores, weapons, outfitters, shipyards, or absent-service behavior as originally implemented by EV Classic.
  - Implementation scaffold: symbolic scenario `system_service_provisioning_scout` records Levo Spaceport's original-runtime-observed commodity/jobs/missions-only boundary (no outfitter/shipyard), then scouts Earth as a full-service Terminal Velocity port and Stardock Alpha as an outfitter/weapons station without shipyard under `sourceLabel=terminal-velocity-service-provisioning-scaffold` / `oracleStatus=classic_runtime_service_matrix_pending`. Godot F10 help now surfaces a service-provisioning scout reminder for newly reached ports, and the deterministic help log verifies `hasServiceProvisioningHelp=true`.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_system_service_provisioning_scout_records_service_matrix_boundaries -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `python3 tools/run_gameplay_scenarios.py --all --pretty`; `./run_godot.sh tv-gameplay-curriculum-help-log`; `./run_godot.sh self-test`.
  - Next action: for each newly reached landable body, capture in-space/landed planet graphics plus landed services and all visible store/weapon/outfitter/shipyard/equipment surfaces before buying/selling; compare against Terminal Velocity active data; provision only bounded source-backed data and otherwise record `missing`, `scaffold`, `needs evidence`, or `not present in original`.

- [ ] Economy-wide buy/sell display and spread rules
  - Status: `candidate / Terminal Velocity cross-market scaffold added`
  - Source: Levo sell-back proves same-port sell equals visible buy price for Levo, but not a universal rule.
  - Implementation scaffold: symbolic scenario `cross_market_trade_spread_scout` exercises one active Terminal Velocity cross-market trade loop: travel from Levo to Sol, buy one 10-ton Food lot at Sol's scaffold buy price (`42`), return to Levo, sell against Levo's observed/source-backed Food sell price (`120`), and end back at Levo with cargo cleared and `780` credits profit. Symbolic scenario `max_hold_trade_route_scout` extends that scaffold to the starting shuttle's full 20-ton hold: buy two Sol Food lots, return to Levo, sell both lots, and verify cargo clears with `1,560` credits profit. Symbolic scenario `trade_route_refuel_profit_loop` keeps that full-hold trade loop but forces empty fuel before the return leg, verifies the profit route blocks on insufficient fuel, refuels at Earth, then returns to Levo and sells both lots under explicit pending-Classic fuel/trade boundaries. The trade events are explicitly labeled `sourceLabel=terminal-velocity-cross-market-trade-scaffold`, `sourceLabel=terminal-velocity-max-hold-trade-scaffold`, or `sourceLabel=terminal-velocity-refuel-trade-route-scaffold` with pending-Classic oracle statuses, so these remain market-spread/refuel scouting scaffolds, not EV Classic global economy claims.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_cross_market_trade_spread_scout_buys_sol_food_and_sells_at_levo -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_max_hold_trade_route_scout_fills_hold_and_sells_two_lots -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_trade_route_refuel_profit_loop_blocks_low_fuel_then_recovers -v`; `python3 tools/run_gameplay_scenarios.py cross_market_trade_spread_scout --pretty`; `python3 tools/run_gameplay_scenarios.py max_hold_trade_route_scout --pretty`; `python3 tools/run_gameplay_scenarios.py trade_route_refuel_profit_loop --pretty`.
  - 2026-06-06 implementation addendum: F10 help now promotes the existing max-hold/refuel trade-route scaffolds into player-facing recovery guidance: when profitable cargo fills the shuttle, preserve a service-port refuel margin before the return jump and sell both lots before taking another cargo job. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasFullHoldTradeRouteRecoveryHelp=true`; `python3 tools/run_gameplay_scenarios.py max_hold_trade_route_scout --pretty` and `python3 tools/run_gameplay_scenarios.py trade_route_refuel_profit_loop --pretty` continue to verify the two-lot sale and low-fuel/refuel recovery under Terminal Velocity scaffold labels.
  - 2026-06-06 implementation addendum: symbolic scenario `trade_route_margin_choice_loop` compares a profitable Sol Food -> Levo return leg (`+78` credits/ton) with an unprofitable Levo Equipment -> Sol candidate (`-210` credits/ton), carries only the positive-margin Food lot, sells it at Levo, and ends with cargo cleared plus `780` credits profit. Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_trade_route_margin_choice_loop_carries_positive_margin_and_skips_bad_cargo -v`; `python3 tools/run_gameplay_scenarios.py trade_route_margin_choice_loop --pretty`. Source labels: `terminal-velocity-trade-margin-choice-scaffold`; oracle status: `trade_margin_choice_pending_classic_runtime_trace`.
  - 2026-06-06 implementation addendum: symbolic scenario `light_freighter_bulk_margin_choice_loop` repeats the margin-choice scaffold after a Light Freighter upgrade: buy the 150-ton ship at Earth, compare profitable Sol Food -> Levo against negative-margin Levo Equipment -> Sol, fill all 150 tons only with positive-margin Food, sell it at Levo, and clear cargo with `22,878` credits remaining. Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_light_freighter_bulk_margin_choice_loop_fills_hold_with_positive_margin -v`; `python3 tools/run_gameplay_scenarios.py light_freighter_bulk_margin_choice_loop --pretty`; broader `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'`, `python3 tools/run_gameplay_scenarios.py --all --pretty`, and `./run_godot.sh self-test` pass with 74 gameplay scenarios. Source labels: `terminal-velocity-light-freighter-bulk-margin-scaffold`; oracle status: `light_freighter_bulk_margin_pending_classic_runtime_trace`.
  - Next action: observe additional ports or decode commodity/economy resources before changing global formula assumptions.

- [ ] EV Classic prefs screen fidelity
  - Status: `implemented / regression tightened`
  - Source: original EV prefs screen visual observation and current Godot selftest output reports `prefScreen=original-ev-classic-observed`.
  - 2026-06-03 correction: the Godot prefs modal weapon-control rows now match the observed EV Classic keybinding labels/keys (`Fire Primary: Tab`, `Fire Secondary: Space`, `Change Secondary: S`, `Next Target: N`, `Closest Target: R`) instead of the earlier mismatched scaffold labels.
  - Strict Play caveat: observed Set Prefs does not include `Strict Play`; do not add it to this prefs modal without direct new-pilot UI evidence.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_prefs_modal_matches_original_ev_classic_observation -v`; `./run_godot.sh self-test`.
  - Next action: keep as regression surface in Godot selftest; add new items here if another prefs control needs exact behavior beyond visual layout.

- [x] EV Classic Strict Play / permadeath pilot option
  - Status: `implemented-default-off`
  - Source: original EV Classic New Pilot dialog capture at `C:\Games\BasiliskII\ev-new-pilot-strict-play-unchecked.png`, plus local EV manual/string evidence and pilot-file structure evidence. User notes correctly that Strict Play should not be used casually because ship destruction can force starting over.
  - Current evidence boundary: setting Strict Play off is directly observed as leaving `Strict Play` unselected in the New Pilot dialog, corresponding to `strictPlayFlag = 0`. The captured dialog text is `If you check this box, when you're dead, you're dead. No reincarnation allowed.`
  - Implementation: `godot_ev/scripts/main.gd` places `Strict Play` in the New Pilot name dialog, defaults it unchecked/off, saves it per pilot as `strict_play`, and restores it when opening a pilot file. `godot_ev/scripts/self_test.gd` emits `strictPlay=off-by-default`; `native_ev/tests/test_model.py` asserts the source-backed contract.
  - Remaining boundary: destructive strict-play death/permadeath semantics remain intentionally untested and unimplemented.

- [ ] Ship facing/frame-order runtime confirmation
  - Status: `needs evidence`
  - Source: decoded 36-facing sprite sheets are integrated, but runtime frame-order/facing behavior still needs observation.
  - Next action: capture deterministic original-runtime turning/facing sequence and compare against Terminal Velocity movement log.

- [ ] Movement tuning: turn rate, acceleration curve, max speed, inertial drift
  - Status: `instrumented / needs original comparison`
  - Source: behavior baseline checklist marks exact original acceleration/max-speed/drift integration as unknown; decoded EV Classic ship fields are wired and Terminal Velocity now emits deterministic movement scenarios.
  - Implementation/instrumentation: `RunGodot.ps1 -MovementLog` emits `right_turn`, `left_turn`, `thrust`, `coast`, and `thrust_right_turn` scenarios with tick count, facing index, angle, velocity, position, and selected ship physics fields. F10 help now surfaces a movement-tuning boundary so players understand arrows/WASD use the current Terminal Velocity inertial flight scaffold, drift/coast are expected, and exact EV Classic acceleration/facing frame order remains pending runtime comparison; `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasMovementTuningHelp=true`.
  - Next action: capture original-runtime acceleration/max-speed/drift against the same scenario shape, then tune Terminal Velocity integration if the deterministic logs diverge.

- [ ] Afterburner acceleration and fuel-use behavior
  - Status: `implemented Terminal Velocity scaffold / needs Classic confirmation`
  - Source: original EV Classic prefs screen proves `Afterburner: Z`; exact acceleration multiplier, fuel/energy consumption, sound, and failure behavior remain unconfirmed.
  - Implementation/instrumentation: Godot treats `Z` as afterburner thrust, applies a Terminal Velocity scaffold multiplier/fuel drain, blocks with visible no-fuel/F5-refuel, landed/L-launch-first, and disabled-player/F8-recovery guidance, and exposes `./run_godot.sh tv-afterburner-log` / `RunGodot.ps1 -AfterburnerLog` with `speedBoosted`, `fuelDrained`, `noFuelBlocked`, `landedBlocked`, `disabledBlocked`, `afterburnerKeyHudVisible`, `afterburnerBlockedGuidanceVisible`, `thrustMultiplier`, and `fuelPerSecond` fields under `sourceLabel=terminal-velocity-afterburner-scaffold` / `oracleStatus=classic_runtime_afterburner_fuel_curve_pending`. F10 help surfaces the player recovery loop: `Z` boosts/drains fuel under the TV scaffold; a blocked/no-fuel afterburner should be recovered by refueling at a landed service port before retrying; a landed afterburner block should be recovered by launching first with `L`, then boosting only after clearing port; and a disabled-player afterburner block should be recovered with the `F8` recovery scaffold before retrying.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_afterburner_log_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `./run_godot.sh tv-gameplay-curriculum-help-log` emitted `hasAfterburnerHelp=true`, `hasAfterburnerFuelRecoveryHelp=true`, `hasAfterburnerLaunchRecoveryHelp=true`, and `hasAfterburnerDisabledRecoveryHelp=true`; `./run_godot.sh tv-afterburner-log` emitted `speedBoosted=true`, `fuelDrained=true`, `noFuelBlocked=true`, `landedBlocked=true`, `disabledBlocked=true`, `afterburnerKeyHudVisible=true`, and `afterburnerBlockedGuidanceVisible=true`.
  - Next action: capture or decode EV Classic afterburner acceleration/fuel behavior before promoting the constants beyond scaffold status.

- [ ] Autopilot behavior and port approach assist
  - Status: `implemented Terminal Velocity scaffold / needs Classic confirmation`
  - Source: original EV Classic prefs screen proves `Autopilot: A`; exact Classic autopilot target choice, speed management, abort conditions, and landing interaction remain unconfirmed.
  - Implementation/instrumentation: Godot now toggles an `A`-key assist that steers and slows toward the nearest port while preserving explicit `terminal-velocity-autopilot-assist-scaffold` labeling; Help documents the scaffold boundary, the launch-first recovery path for landed `A` blocks, the no-port recovery path, and the disabled-player recovery path (`F8` before retrying `A`), and `./run_godot.sh tv-autopilot-log` / `RunGodot.ps1 -AutopilotLog` emits `TV_AUTOPILOT_EVENT` fields for engaged/disengaged state, approach-distance improvement, approach-speed reduction, landed autopilot blocked guidance, disabled-player toggle blocked guidance, mid-assist disabled-player auto-disengage, no-port auto-disengage, and target body. The gameplay curriculum help probe now also asserts `hasAutopilotHelp=true`, `hasAutopilotLaunchRecoveryHelp=true`, `hasAutopilotNoPortRecoveryHelp=true`, and `hasAutopilotDisabledRecoveryHelp=true` so the visible F10 guidance remains covered as the help surface grows.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_autopilot_assist_log_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `./run_godot.sh tv-autopilot-log` emits `autopilotLandedBlocked=true`. 2026-06-05 addendum: the autopilot probe verifies `autopilotDisabledBlocked=true`, preserving the disabled-player action gate for the `A` assist, verifies `autopilotDisabledDisengaged=true`, stopping an already-engaged TV autopilot scaffold if the player becomes disabled mid-assist, and now verifies `autopilotNoPortDisengaged=true`, stopping an already-engaged scaffold when the current system has no landable port target. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies the visible help boundary, including `hasAutopilotLaunchRecoveryHelp=true`, `hasAutopilotNoPortRecoveryHelp=true`, and `hasAutopilotDisabledRecoveryHelp=true`. Keep all of this labeled as Terminal Velocity scaffold pending Classic death/reload/autopilot evidence.
  - Next action: capture or decode EV Classic autopilot behavior before promoting target/speed constants or UI wording beyond scaffold status.

- [ ] Land/takeoff and hyperspace timing/sound/animation fidelity
  - Status: `instrumented / needs original comparison`
  - Source: baseline checklist marks landing/hyperspace loop partial or unknown; 2026-05-20 recovery/continuation passes found post-movement key automation no longer delivered visible `J`/`I`/`M`/`A` effects from the `Hyperspace / Rigel` state, even though arrow acceleration and animation still worked. User correction: the player must be far enough from the planet/system center before hyperspace can start, so jump attempts must be iterated after sustained movement rather than abandoned after one try. Follow-up iteration used multiple longer acceleration segments plus `J` retries; no successful transit or distance-failure message was captured.
  - Implementation/instrumentation: `RunGodot.ps1 -TravelEventLog` emits deterministic start, land request, leave, hyper mode, hyper select, and jump events from current Terminal Velocity behavior. F10 help now surfaces a travel-timing recovery boundary for this log: after `L` launch/leave or `H`/`\\` hyper-select, wait for the HUD travel event state before retrying `J` or landing; this remains a TV event-log scaffold pending exact Classic transition timing/sound evidence.
  - Verification addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `./run_godot.sh tv-gameplay-curriculum-help-log` emitted `hasTravelTimingRecoveryHelp=true`; `./run_godot.sh tv-travel-event-log` emitted start/land/leave/hyper-mode/hyper-select/jump event states while retaining the too-close jump block.
  - Next action: use a fresh non-strict original EV session/pilot or alternate input route that first proves reliable letter-hotkey receipt (`I`, `M`, `L`, `H`, `J`, `A`) before and after movement, then capture successful hyperspace transit and compare to Godot event logs.

- [ ] Landed service/button matrix: bar, mission computer, commodities, outfitter, shipyard, gambling
  - Status: `instrumented / needs original click-through`
  - Source: user-requested EV mirroring pass for clicking available landed buttons/options; Levo original-runtime observation only proves `Spaceport Bar`, `Mission Computer`, `Commodity Exchange`, and `Leave` are visible there, with no outfitter button.
  - Implementation/instrumentation: `RunGodot.ps1 -LandedUiMatrix` emits each TV body’s visible landed buttons, services, item counts, mutating actions, and `observationGuard=before_after_capture_required`. Terminal Velocity now also exposes service availability in play: landed panels show `Refuel: F5 available` or `Refuel: unavailable`, the HUD shows current/max fuel, F5 refuels when local service exists, and blocked/success refuel attempts flow through the recent message surface. F10 help now promotes the verified `outfitter_purchase_guardrail_recovery_loop` into player guidance: if `B` is blocked, check landed service, selected listing, credits, reputation/legal gates, and cargo transfer limits before retrying; this remains a Terminal Velocity scaffold pending exact Classic store/refusal UI evidence. F10 help also promotes the deterministic `tv-repair-service-log` blocked-action path into repair recovery guidance: if `F7` is blocked, check landed repair service, damaged hull, and credits before retrying; this is a Terminal Velocity repair-service scaffold pending exact Classic repair UI/service evidence.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_landing_panels_are_actionable native_ev.tests.test_model.NativeEvModelTests.test_godot_route_jump_land_refuel_autoresearch_log_contract -v`; `./run_godot.sh tv-route-land-refuel-log` emitted `refuelAvailable=true`, `refuelSucceeded=true`, and `travelLoopComplete=true`. Addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `./run_godot.sh tv-gameplay-curriculum-help-log` emitted `hasOutfitterGuardrailHelp=true`; `python3 tools/run_gameplay_scenarios.py outfitter_purchase_guardrail_recovery_loop --pretty` emitted `success=true` with blocked no-service/not-landed/insufficient-credit recovery checks passing. Repair addendum: `./run_godot.sh tv-gameplay-curriculum-help-log` emitted `hasRepairRecoveryHelp=true`; `./run_godot.sh tv-repair-service-log` emitted `inSpaceBlocked=true`, `alreadyFullBlocked=true`, and `insufficientBlocked=true`.
  - Next action: run bounded original EV click-through starting at Levo and then an outfitter/shipyard/gambling-capable early port, using disposable/non-strict state and before/after captures for every mutating option.

- [ ] Basic combat fidelity: fire rate, projectile speed/lifetime/damage, target selection, explosions
  - Status: `implemented Terminal Velocity source-mined combat scaffold / stock weapon name-field crosswalk added / exact Classic combat timing and effects need evidence`
  - Source: local `native_ev/data/weapons.json`/`ships.json`; EV Classic Resource Bible `wëap` fields for `Reload`, `Count`, `MassDmg`, `EnergyDmg`, `Guidance`, and `Speed`; EV Classic Resource Bible `oütf` fields for `ModType`/`ModVal` linking sale/ammo outfits to `wëap` IDs; decoded original `weapon-like` and `outfit-like` records in `native_ev/data/sourced_ev_structures.json`; derived crosswalk `native_ev/data/sourced_ev_weapons.json` generated by `tools/extract_ev_weapon_semantics.py` maps stock names such as Laser Cannon, Neutron Blaster, Torp. Launcher, Missile Rack, and Fighter Bay through outfit records while preserving unresolved links such as Forklift -> 191.
  - Implementation: Godot now makes Tab primary fire playable and reciprocated: it spawns a projectile toward the selected/closest contact, advances lifetime/collision, applies separate Bible-named shield/hull damage formulas (`MassDmg/4 + EnergyDmg` while shields are up; `MassDmg + EnergyDmg/4` after shields are down), tracks player shields/hull plus `ShieldRe`-paced recharge, fires one NPC retaliation projectile, draws projectile blips, target S/H readouts, and a small Terminal Velocity explosion visual scaffold when a target is disabled, saves/loads player shields/hull, and exposes `./run_godot.sh tv-combat-log` / `RunGodot.ps1 -CombatLog` with `sourceLabel=terminal-velocity-source-mined-combat-scaffold`, `oracleStatus=classic_runtime_weapon_timing_pending`, and stock provenance fields (`sourceResourceId`, `sourceStockName`, `sourceMassDmg`, `sourceEnergyDmg`, `sourceReload`, `sourceCount`) pulled from the source-mapped weapon manifest. Destroyed contacts now also drop a small green Terminal Velocity cargo-salvage scaffold marker that can be scooped when cargo capacity is available, leaves the pickup in space with visible guidance when the hold is full, persists blocked in-space salvage through the pilot save/resume path, and exposes `./run_godot.sh tv-cargo-salvage-log` / `RunGodot.ps1 -CargoSalvageLog` with `sourceLabel=terminal-velocity-combat-salvage-scaffold` and `oracleStatus=classic_runtime_loot_cargo_behavior_pending` so loot/cargo behavior remains explicitly unconfirmed against Classic. Target selection now also has a deterministic `./run_godot.sh tv-target-selection-log` / `RunGodot.ps1 -TargetSelectionLog` surface that verifies `N`-style cycle and `R` closest-contact status updates under `sourceLabel=terminal-velocity-target-selection-scaffold` / `oracleStatus=classic_runtime_target_selection_pending`. Secondary weapons now have a bounded Terminal Velocity scaffold: SPACE still reports no secondary loaded from the EV Classic start-state-compatible default, `S` cycles installed non-primary weapons, installed secondary fire uses the same source-mined damage/reload fields with an independent cooldown, Player Info changes from the original-runtime-observed `No Secondary Weapon` start-state line to the selected installed secondary weapon/source crosswalk once a secondary is installed, help text names Space/S secondary controls, and `./run_godot.sh tv-secondary-weapon-log` / `RunGodot.ps1 -SecondaryWeaponLog` emits `TV_SECONDARY_WEAPON_EVENT` with `secondaryInventoryEmptyVisible`, `secondaryInventoryLoadedVisible`, `sourceLabel=terminal-velocity-secondary-weapon-scaffold`, and `oracleStatus=classic_runtime_secondary_weapon_behavior_pending`. The deterministic combat log now also prepares a final-hit destroy state and reports `destroyScenarioPrepared=true`, `targetDestroyed=true`, `explosionTriggered=true`, and `explosionSourceLabel=terminal-velocity-explosion-visual-scaffold` while keeping exact Classic explosion timing/frames pending. It additionally prepares a player-disabled retaliation hit and reports `playerDisableRetaliationFired=true`, `playerDisabled=true`, `playerDisabledStatusVisible=true`, `playerDisabledExplosion=true`, `disabledFireBlocked=true`, `disabledSecondaryBlocked=true`, `disabledChangeSecondaryBlocked=true`, `disabledAutopilotGuidance=true`, `disabledHyperModeGuidance=true`, `disabledHyperSelectGuidance=true`, `disabledMovementBlocked=true`, `disabledSaveBlocked=true`, `disabledServiceRefuelBlocked=true`, `disabledServiceRepairBlocked=true`, `disabledServiceClemencyBlocked=true`, `disabledMissionAcceptBlocked=true`, `disabledTradeBuyBlocked=true`, `disabledTradeSellBlocked=true`, `disabledOutfitBuyBlocked=true`, `disabledShipBuyBlocked=true`, `disabledJumpGuidance=true`, and `disabledLandGuidance=true`; disabled service/trade/mission/purchase helpers now use the shared `_disabled_player_action_blocked()` guard before mutating state, and the log still verifies an F8 recovery scaffold with `recoveryTriggered=true`, `playerRecovered=true`, and `recoveryStatusVisible=true`, keeping exact EV Classic destruction/reload and Strict Play death semantics pending safe original-runtime confirmation. Runtime `weapons.json` now carries source stock IDs/names/fields for the current TV weapons and applies the decoded stock `MassDmg`, `EnergyDmg`, and `Reload` fields to playable damage/reload values while leaving projectile speed/lifetime/`Count` semantics explicitly TV-tuned pending runtime observation. Primary fire now enforces the source-mapped reload cadence as a player-visible cooldown guardrail; immediate repeat Tab presses report `Primary weapon reloading; wait for source-backed reload cadence`, destroyed selected contacts are blocked from absorbing extra shots and retarget to the next active contact with visible feedback, while uninstalled secondary fire remains blocked with explicit no-secondary guidance.
  - Implementation addendum: secondary-weapon ownership no longer overrides the ship's primary weapon slot; `_primary_weapon_stats()` preserves the source ship `weaponId` for Tab/primary fire while owned non-primary weapons remain selected/fired through the secondary path. `./run_godot.sh tv-secondary-weapon-log` reports `primaryWeaponPreserved=true` and `sourcePrimaryId=laser_cannon` so the scaffold catches accidental primary/secondary slot coupling. The help overlay now includes secondary-weapon recovery guidance for the source-observed `No Secondary Weapon` starting HUD: install a secondary, press `S` to select it, then fire with `Space` after the source-backed reload cadence. The same probe now reports decoded/source-backed secondary sound playback (`secondaryWeaponSound=weapon_cannon`, `secondaryWeaponSoundPlayed=true`, `soundSourceLabel=decoded-resource-backed-sound-binding`, `soundOracleStatus=classic_runtime_sound_timing_pending`) and a compact in-flight HUD fragment (`secondaryHudVisible=true`, `Secondary: Pulse Cannon (reload 149 frames)`) so selected secondary/reload state is visible without opening Player Info. Exact Classic secondary HUD/arming semantics remain pending original-runtime/source evidence.
  - Implementation addendum: symbolic scenario `weapon_inventory_stack_recovery_loop` buys two Pulse Cannons at Sirius Station and verifies `ownedWeapons.pulse_cannon == 2` plus credits `7200`, preserving `sourceLabel=terminal-velocity-weapon-inventory-stack-scaffold` / `oracleStatus=classic_runtime_multiple_weapon_purchase_inventory_pending` so repeated secondary purchases are playable without claiming exact Classic inventory UI semantics.
  - Implementation addendum: the combat guardrail probe now records decoded primary sound coverage (`primaryWeaponSound=weapon_laser`, `primaryWeaponSoundPlayCount=2`, `primaryWeaponSoundPlayedForValidShots=true`) so valid first/after-cooldown Tab shots are audibly bound while an immediate reload-blocked shot does not create an extra weapon-fire sound event.
  - Implementation addendum: disabled NPC contacts now pay a small Terminal Velocity combat-reward scaffold (`25` credits) once per target, record `combat_reward_history` with the disabled contact label, persist that history through pilot save/load, expose the reward history in Player Info as `Combat rewards: N disable(s), N credits — TV combat-reward scaffold; Classic bounty behavior pending` plus the last rewarded contact/credits, append a compact in-flight HUD fragment (`Rewards: N disable(s)/N cr`) after rewards exist, and set immediate disable feedback such as `Contact 1 disabled; reward +25 cr — TV scaffold, Classic bounty pending` so the player sees the local reward without opening Player Info. `./run_godot.sh tv-combat-log` reports `combatRewardPaid`, `combatRewardAmount`, `combatRewardRecorded`, `combatRewardSaved`, `combatRewardInventoryVisible`, `combatRewardHudVisible`, `combatRewardLastTargetVisible`, `combatRewardStatusVisible`, and before/after credits under `sourceLabel=terminal-velocity-combat-reward-scaffold` / `oracleStatus=classic_runtime_combat_reward_behavior_pending`. F10 help also surfaces legal-status guidance (`Legal status: Player Info shows current government/legal stance; hostile patrol fire worsens the TV scaffold and landed C buys clemency when eligible.`), and `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLegalStatusHelp=true`. Exact Classic bounty/reward/legal behavior remains pending original-runtime/resource evidence.
  - Implementation addendum: F10 help now surfaces combat-reward recovery guidance for the existing disable-credit scaffold: after disabling a target, verify Player Info/HUD reward history and legal deltas before assuming a Classic bounty payout. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasCombatRewardRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-combat-reward-scaffold` / `oracleStatus=classic_runtime_combat_reward_behavior_pending`.
  - Implementation addendum: projectile motion now has an independent deterministic probe, `./run_godot.sh tv-projectile-motion-log` / `RunGodot.ps1 -ProjectileMotionLog`, that verifies a primary projectile spawns, moves, damages the selected contact, and expires when forced through an out-of-range lifetime path. The log reports `initialProjectileSpeed`, source `Speed`/`Count`/lifetime fields, and keeps `sourceLabel=terminal-velocity-projectile-motion-scaffold` / `oracleStatus=classic_runtime_projectile_motion_pending`, because exact Classic projectile motion and frame timing are still unconfirmed. F10 help now surfaces the projectile-motion recovery path: keep the target selected, watch scanner damage, and avoid interpreting source-mined travel/lifetime delay as a broken shot before Classic timing is captured.
  - Implementation addendum: explosion timing now has an independent deterministic probe, `./run_godot.sh tv-explosion-log` / `RunGodot.ps1 -ExplosionLog`, that destroys a selected contact, verifies an explosion event is created, advances through a visible/life-decrement phase, and confirms the scaffold expires. The log keeps `sourceLabel=terminal-velocity-explosion-visual-scaffold` / `oracleStatus=classic_runtime_explosion_timing_pending`, because exact Classic explosion frames, duration, sound, and death/reload semantics still need original-runtime/source evidence. F10 help now surfaces the explosion-timing recovery path: wait for the short TV explosion scaffold and scanner/HUD update before retargeting or assuming cleanup/reward failed.
  - Implementation addendum: NPC retaliation cadence now has an independent deterministic probe, `./run_godot.sh tv-retaliation-log` / `RunGodot.ps1 -RetaliationLog`, that verifies a hostile contact can fire back, blocks an immediate second retaliatory shot behind the same source-mined reload field, clears the cooldown after the measured cadence, fires again, and damages the player. The log keeps `sourceLabel=terminal-velocity-npc-retaliation-scaffold` / `oracleStatus=classic_runtime_ai_retaliation_cadence_pending`, because exact Classic AI target selection, aggression thresholds, fire cadence, and projectile behavior are pending original-runtime/source confirmation.
  - Implementation addendum: target selection now skips disabled/destroyed contacts for both next-target cycling and closest-target selection, reports `No active scanner targets` when every contact is disabled, includes a selected-target scanner detail line with contact label, range, and shield/hull (`S/H`) in the in-flight scanner HUD plus `N`/`R` status messages, and exposes `destroyedTargetSkippedByCycle`, `destroyedTargetSkippedByClosest`, `cycledStatusHasStats`, `closestStatusHasStats`, `scannerTargetDetailVisible`, `noActiveScannerStatusVisible`, and `liveTargetCount` through `./run_godot.sh tv-target-selection-log` / `RunGodot.ps1 -TargetSelectionLog`. F10 help now surfaces the no-active-target retarget recovery path: disabled/destroyed contacts are skipped, so find live contacts or leave the route before retrying N/R. This keeps combat flow playable after kills while retaining `sourceLabel=terminal-velocity-target-selection-scaffold` / `oracleStatus=classic_runtime_target_selection_pending` until original EV target retarget/scanner semantics are captured.
  - Implementation addendum: combat now plays decoded/source-backed sound bindings for player primary fire, NPC retaliation fire, and ship explosion/player-disabled events instead of reusing the UI click sound. `./run_godot.sh tv-combat-log` emits `TV_SOUND_EVENT` with `primaryWeaponSound=weapon_laser`, `npcWeaponSound=weapon_cannon`, and `explosionSound=ship_explodes` under `sourceLabel=decoded-resource-backed-sound-binding` / `oracleStatus=classic_runtime_sound_timing_pending`; exact Classic sound timing/mix remains pending runtime/source comparison.
  - Implementation addendum: combat sound playback now has a preference guardrail tied to the observed Set Prefs sound-volume surface. `./run_godot.sh tv-combat-guardrail-log` mutes `pref_sound_on`, fires a valid primary shot, and verifies `soundPreferenceMuted=true`, `mutedSoundEventCount=0`, and `soundPreferenceSuppressesCombatSound=true` while keeping the decoded sound-binding labels pending exact Classic volume semantics. F10 help now tells players to reopen Set Prefs if combat is silent before treating weapons as broken.
  - Implementation addendum: F10 help now surfaces combat-cadence recovery guidance for source-mined reload timing: immediate repeated fire attempts can be blocked by the cooldown/HUD message, so players should wait for the reload cadence before treating weapons as broken. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasCombatCadenceHelp=true` alongside the existing combat sound preference help. Exact Classic timing remains pending original-runtime comparison.
  - Implementation addendum: F10 help now surfaces NPC-retaliation recovery guidance for the existing source-mined retaliation cadence scaffold: hostile contacts can fire back after their cooldown, so players should watch Player Info shields/hull, break off to repair, and reserve F8 for disabled-player recovery. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasNpcRetaliationRecoveryHelp=true` and `./run_godot.sh tv-retaliation-log` verifies the underlying first/immediate-blocked/after-cooldown retaliation cadence. Exact Classic AI target selection, aggression thresholds, and cadence remain pending original-runtime/source confirmation.
  - Implementation addendum: disabled-player behavior now has an independent deterministic probe, `./run_godot.sh tv-player-disabled-log` / `RunGodot.ps1 -PlayerDisabledLog`, that forces the player ship disabled, verifies disabled-status/explosion/sound signaling, blocks fire/jump/movement/launch, then verifies the F8 Terminal Velocity recovery scaffold restores shields and hull. The log keeps `sourceLabel=terminal-velocity-player-disabled-scaffold` / `oracleStatus=classic_runtime_player_death_pending_strict_play_safe_trace`, because exact Classic death/reload/permadeath semantics still need safe original-runtime evidence.
  - Implementation addendum: F10 help now calls out disabled-player recovery as a separate playable scaffold: while the player ship is disabled, flight/combat/service/trade/mission actions stay blocked until the F8 recovery scaffold is used. This is paired with the existing `disabled_player_recovery_loop` and `tv-player-disabled-log` coverage and remains pending Classic death/reload evidence.
  - Implementation addendum: player shield recharge now has an independent deterministic probe, `./run_godot.sh tv-shield-recharge-log` / `RunGodot.ps1 -ShieldRechargeLog`, that starts below max shields, confirms no recharge before the source-mined `shieldRecharge` frame cadence, confirms one and then multiple recharge ticks, verifies recharge is blocked while the player hull is disabled, and shows current shields/hull together in the Player Info combat-readiness line. The log keeps `sourceLabel=decoded-resource-backed-ship-shield-recharge-scaffold` / `oracleStatus=classic_runtime_shield_recharge_timing_pending`; exact Classic shield tick timing/effects remain pending original-runtime confirmation.
  - Implementation addendum: F10 help now surfaces shield-recharge recovery as the follow-up to NPC retaliation damage: break off, wait for the Player Info source-mined shield-recharge cadence before re-engaging, and use F8 only for disabled hull recovery. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasShieldRechargeRecoveryHelp=true`, and `./run_godot.sh tv-shield-recharge-log` verifies `combatReadinessVisible=true` while retaining `sourceLabel=decoded-resource-backed-ship-shield-recharge-scaffold` / `oracleStatus=classic_runtime_shield_recharge_timing_pending`.
  - Implementation addendum: Player Info, the in-space HUD, and scanner now surface pending in-space salvage pickup count/tons or green salvage blips so a full-hold player can see that TV-scaffold combat loot remains recoverable after save/resume; F10 help now also surfaces salvage recovery guidance for that full-hold case: sell or deliver cargo, then return to the HUD/Player Info marker before leaving the system. `./run_godot.sh tv-cargo-salvage-log` reports `salvageInventoryVisible=true`, `salvageHudVisible=true`, and `salvageScannerVisible=true`, and `./run_godot.sh tv-gameplay-curriculum-help-log` reports `hasSalvageRecoveryHelp=true`, while retaining `sourceLabel=terminal-velocity-combat-salvage-scaffold` / `oracleStatus=classic_runtime_loot_cargo_behavior_pending`.
  - Verification addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_target_selection_log_contract -v`; `./run_godot.sh tv-target-selection-log` emitted `destroyedTargetSkippedByCycle=true`, `destroyedTargetSkippedByClosest=true`, `liveTargetCount=3`, and `noActiveScannerStatusVisible=true`.
  - Verification addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_retaliation_log_contract -v`; `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'`; `python3 tools/run_gameplay_scenarios.py --all --pretty`; `./run_godot.sh tv-retaliation-log` emitted `firstRetaliationFired=true`, `immediateSecondRetaliationBlocked=true`, `npcCooldownFrames=149`, `npcCooldownCleared=true`, `retaliationAfterCooldownFired=true`, `playerDamagedByRetaliation=true`, and `sourceReload=149`; `./run_godot.sh self-test` emitted `GODOT SELFTEST OK profile=classic gameplayScenarios=20`; `git diff --check`.
  - Verification addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract native_ev.tests.test_model.NativeEvModelTests.test_godot_combat_guardrail_log_contract -v`; `./run_godot.sh tv-combat-guardrail-log` emitted `soundPreferenceMuted=true`, `mutedSoundEventCount=0`, and `soundPreferenceSuppressesCombatSound=true`; `./run_godot.sh tv-gameplay-curriculum-help-log` emitted `hasCombatSoundPrefsHelp=true`; full native discovery, full gameplay scenarios, Godot self-test, `git diff --check`, and `python3 -m json.tool native_ev/data/help_overlay.json` passed in the 2026-06-05 combat-sound-pref slice.
  - Verification addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `./run_godot.sh tv-gameplay-curriculum-help-log` emitted `hasCombatCadenceHelp=true` in the combat-cadence help slice. Shield-recharge recovery addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract native_ev.tests.test_model.NativeEvModelTests.test_godot_shield_recharge_log_contract -v`; `./run_godot.sh tv-gameplay-curriculum-help-log` emitted `hasShieldRechargeRecoveryHelp=true`; `./run_godot.sh tv-shield-recharge-log` emitted `combatReadinessVisible=true`.
  - Next action: capture original runtime combat behavior for exact fire cadence, projectile motion, explosions, AI retaliation cadence, rewards, deaths, and legal consequences before promoting the scaffold to Classic fidelity.

- [ ] EV-family gameplay learning source integration
  - Status: `candidate`
  - Source: `docs/research/ev-family-gameplay-source-deep-dive.md`; user guidance on 2026-05-21 that EV Classic, Escape Velocity: Override, and EV Nova share basic gameplay, so Override/Nova sources should be mined for transferable learning. Deeper source pass used locally extracted EVN walkthrough index/faction pages and EV Nova Bible text under `/tmp/ev-source-deep-dive-20260521/text/`.
  - Scope: use EV-family manuals, overlays, data-derived walkthroughs, community guides, wikis, archives, and implementation references to shape Terminal Velocity gameplay learning surfaces without treating non-Classic sources as exact EV Classic runtime truth.
  - Design direction: preserve the EV-family core loop of landing, missions/trade, map route planning, fuel/risk management, combat survival, ship/outfit upgrades, faction/reputation progression, and harder route/story unlocks. Prioritize player-facing learning surfaces: keyboard overlay/manual, new-pilot tutorial, mission log details, map overlays, ship/outfit comparisons, spoiler-light faction hints, trade-route notes, and reputation/legal feedback.
  - Next action: convert the source-grounded model in `docs/research/ev-family-gameplay-source-deep-dive.md` into small implementation plans or UI/data tickets, starting with mission state/log, map arrows/fuel feedback, per-government legal state, cargo reservation, and ship/outfit comparison.
  - Fidelity boundary: exact Classic behavior still requires original runtime observation, decoded EV Classic resources, manuals/docs, or Terminal Velocity instrumentation; EV Override/Nova/community/open-source sources are hypothesis/design-learning inputs unless independently verified.

- [ ] Mission state/log and mission-surface model
  - Status: `implemented Terminal Velocity scaffold / abort semantics pending Classic confirmation`
  - Source: EVN walkthrough index mission key and EV Nova Bible mission fields, recorded in `docs/research/ev-family-gameplay-source-deep-dive.md`.
  - 2026-06-04 decoded Classic addendum: `tools/extract_ev_mission_semantics.py` now maps EV Classic Resource Bible `mïsn` fields through `Flags` into `native_ev/data/sourced_ev_missions.json`: description resource selectors, `TimeLimit`, `CanAbort`, `AvailBitClr`, auxiliary ships, second completion bit, and named mission flags. The regenerated stock manifest contains 116 missions, 15 non-abortable missions, and 99 missions with at least one known flag; this is decoded-resource/manual-backed evidence for future mission lifecycle/scenario work, not exact runtime dialog wording.
  - Scope: model mission availability, offer surface, accept/success/failure/abort state, travel destination, return destination, special ship goals, deadline, cargo/passenger reservation, pay, combat/legal/ship gates, and faction/legal consequences. Preserve distinct surfaces such as Mission Computer, Bar, Trading Center, Shipyard, and Outfitter instead of flattening all jobs into one list.
  - Implementation: symbolic scenario `mission_abort_releases_reserved_cargo` now accepts `intro_courier_earth_hera`, aborts it without completion, releases its 3 reserved cargo tons, records `abortedJobs`, and labels the trace `sourceLabel=terminal-velocity-mission-abort-scaffold` / `oracleStatus=mission_abort_pending_classic_runtime_or_manual_trace`. Symbolic scenario `mission_abort_reaccept_delivery_loop` verifies recovery after an abort: it accepts the intro courier, aborts and releases the reserved 3 tons, reaccepts the same TV scaffold mission, delivers it at Luna, preserves `abortedJobs` history, clears active/reserved cargo, and records `sourceLabel=terminal-velocity-mission-abort-reaccept-scaffold` / `oracleStatus=mission_abort_reaccept_pending_classic_runtime_or_manual_trace`. Godot exposes the same scaffold through `X` to abort an active mission, persists `aborted_mission_history`, resets it for deterministic automated logs, and provides `./run_godot.sh tv-mission-abort-log` / `RunGodot.ps1 -MissionAbortLog`, which verifies no-active abort guidance before acceptance, mission acceptance, abort, reserved-cargo release, repeat-abort blocked guidance after removal, empty active/completed mission state, and latest abort metadata. Symbolic scenario `mission_deadline_failure_scaffold` now models the EV Classic Resource Bible-backed failure shape for timed missions: advance past `TimeLimit`, release reserved cargo, record a `FailBitSet`-style failure flag, and apply the documented half-`CompReward` government reputation penalty while retaining `oracleStatus=deadline_failure_runtime_ui_pending_classic_trace` for exact original UI/timing confirmation. Godot now exposes this through `./run_godot.sh tv-mission-deadline-failure-log` / `RunGodot.ps1 -MissionDeadlineFailureLog`, records `failed_mission_history`, releases reserved cargo, sets `fail_mission_bit_42`, and applies the half-`completionReward` Federation penalty with the same source/oracle labels. The mission log helper now shows completed, aborted, and failed history even when no active missions remain; `./run_godot.sh tv-mission-log-history-log` verifies the no-active post-transition line list and now includes visible deadline counters/source-boundary text for failed jobs as a Terminal Velocity helper scaffold. Active mission detail lines now also show deadline counters before transition (`accepted day`, `current day`, `limit`, remaining days), active abort guidance (`X` abort, reserved cargo release), and source-boundary lines; the Player Info overlay (`P`) also summarizes active mission title/destination, remaining deadline days, cargo/reward, and source boundary, with `./run_godot.sh tv-active-mission-deadline-log` verifying both the mission-log and Player Info line contracts.
  - Verification/addendum: `./run_godot.sh tv-mission-chain-offer-log` / `RunGodot.ps1 -MissionChainOfferLog` now verifies the intro courier completion unlocks Luna's `frontier_sample_hera_freeport` offer with selected-offer helper details under `sourceLabel=terminal-velocity-observed` / `oracleStatus=terminal_velocity_eval_pending_original_trace`. `./run_godot.sh tv-mission-offer-scan-log` now also verifies first-offer Mission Computer helper details for deadline status (`Offer deadline: not listed in current TV mission data`), visible story/legal/reputation requirement summaries, and story start/completion/choice-group/reputation-event/next-contract flags. `./run_godot.sh tv-mission-alignment-branch-log` / `RunGodot.ps1 -MissionAlignmentBranchLog` continues that deterministic chain through Sirius Station, verifies both chapter-one alignment branch offers (`federation_report_freeport`, `freeport_pact_smugglers`) are visible with shared `chapter_one_alignment` choice-group metadata, exposes selected Federation branch helper detail lines before acceptance (`selectedBranchOfferDetailsVisible=true`), accepts the Federation branch and verifies the Freeport branch disappears from available offers, then replays the chain from a reset deterministic state to accept the Freeport branch and verify the Federation branch disappears through the Terminal Velocity choice-group scaffold (`choiceBoundary=terminal_velocity_choice_group_scaffold_exact_classic_branch_ui_pending`) under the same source/oracle boundary.
  - Implementation addendum: symbolic scenario `alignment_story_prereq_recovery` now verifies the chapter-one branch offers are hidden at Sirius Station when the frontier sample story prerequisite is absent, then reappear once `frontier_samples_delivered` is present. Symbolic scenario `alignment_offer_requirement_recovery` now verifies the same branch offers are hidden just below their manifest reputation/legal thresholds, then reappear at the exact active Terminal Velocity thresholds (`Federation` reputation `5` / legal `-20`, `Independent` reputation `7` / legal `-90`). Both retain `sourceLabel=terminal-velocity-observed` / `oracleStatus=terminal_velocity_eval_pending_original_trace`. Godot F10 help now surfaces alignment-gate recovery guidance so players know to inspect Mission Computer details for story flags, reputation/legal thresholds, and branch-choice boundaries before hauling cargo. A 2026-06-05 help addendum surfaces the existing mission-chain lock probe in the same F10 surface: if a chain offer is locked or excluded, read the Mission Computer reason for missing story flags or active exclusions before hauling more cargo; `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasMissionStoryGateRecoveryHelp=true`, paired with `./run_godot.sh tv-mission-chain-lock-log` verifying `lockedStoryReasonVisible=true`.
  - Implementation addendum: symbolic scenario `freeport_alignment_delivery_loop` now exercises the alternate chapter-one alignment branch: it accepts `freeport_pact_smugglers`, delivers at Luna for `3000` credits, records `alignment_freeport` / `freeport_network_asset`, and verifies `federation_report_freeport` remains blocked afterward under `sourceLabel=terminal-velocity-mission-scaffold` / `oracleStatus=mission_behavior_pending_classic_runtime_trace`. Symbolic scenario `alignment_completion_offer_scan_guardrail` now checks completed Federation and Freeport branch states by scanning Sirius Station offers and verifying the closed alignment choice group no longer presents either branch while preserving the still-available non-branch return contract and pending-Classic UI boundary labels. Symbolic scenario `alignment_return_contract_offer_timing_guardrail` now records the active Terminal Velocity offer timing: Sirius Station presents the non-branch `freeport_return_earth` contract alongside both still-open chapter-one alignment branch offers after `frontier_samples_delivered`, then preserves only the return contract after a branch completion; this is labeled `terminal-velocity-observed` / `terminal_velocity_eval_pending_original_trace` until Classic mission UI/timing evidence exists. Symbolic scenario `alignment_completion_return_contract_loop` now verifies that the non-branch `freeport_return_earth` contract can still be accepted and delivered after either completed alignment branch, preserving cargo release and mission scaffold source boundaries. Godot F10 help now surfaces that a non-branch return contract may remain after a chapter branch choice, so players scan the Mission Computer before assuming the chain is complete; the help-log probe reports `hasAlignmentReturnHelp=true`.
  - Implementation addendum: symbolic scenario `mission_deadline_failure_recovery_loop` now exercises a post-deadline recovery path: fail `deadline_dispatch_failure_probe`, preserve `failedJobs`, `fail_mission_bit_42`, cargo release, and the EV Classic Resource Bible-backed half-`CompReward` Federation reputation penalty, then accept and complete `deadline_recovery_followup` under `sourceLabel=terminal-velocity-mission-deadline-recovery-scaffold` / `oracleStatus=deadline_failure_recovery_pending_classic_runtime_or_manual_trace`. Exact Classic reoffer/follow-up UI remains pending original-runtime/manual evidence.
  - Implementation addendum: symbolic scenario `mission_deadline_trade_carryover_loop` now verifies deadline failure releases only mission-reserved cargo while preserving non-mission trade cargo: the pilot accepts a timed dispatch, buys one Sol Food lot with spare hold, misses the deadline, returns to Levo, sells the preserved trade lot for the active TV market profit, and retains `failedJobs` / `fail_mission_bit_42` / reputation penalty under `sourceLabel=terminal-velocity-mission-deadline-trade-carryover-scaffold` / `oracleStatus=deadline_failure_trade_carryover_pending_classic_runtime_or_manual_trace`. Godot F10 help now exposes a deadline-recovery reminder for that verified TV scaffold: failed timed missions release reserved cargo, trade cargo may remain recoverable, and the player should sell cargo or scan for follow-up contracts before abandoning the route. Exact Classic failure/trade-carryover UI and consequences remain pending original-runtime/manual evidence.
  - Implementation addendum: symbolic scenario `mission_deadline_sequential_failures_loop` now verifies two active timed deliveries expiring in the same deadline pass: the pilot accepts `deadline_dispatch_failure_probe` and `deadline_second_failure_probe`, advances past both deadlines, releases all 5 reserved cargo tons, records `fail_mission_bit_42` and `fail_mission_bit_43`, moves both jobs to `failedJobs`, and applies cumulative EV Classic Resource Bible-backed half-`CompReward` Federation reputation penalties (`-3`, `-2`) while keeping exact Classic multi-failure UI/timing under `oracleStatus=deadline_failure_runtime_ui_pending_classic_trace`. Godot now exposes the same multi-deadline guardrail through `./run_godot.sh tv-mission-deadline-sequential-log` / `RunGodot.ps1 -MissionDeadlineSequentialLog`, logging `bothDeadlineFailuresRecorded=true`, `reservedCargoReleased=true`, `failureFlagsSet=true`, and `cumulativeReputationPenaltyApplied=true` under `sourceLabel=terminal-velocity-mission-deadline-sequential-failures-scaffold` / `oracleStatus=deadline_sequential_failures_pending_classic_runtime_or_manual_trace`.
  - Implementation addendum: symbolic scenario `mission_deadline_abort_prevents_failure_loop` now verifies the active Terminal Velocity abort guardrail for a timed dispatch: accept `deadline_dispatch_failure_probe`, abort it before expiry, release the 3 reserved cargo tons, advance beyond the original deadline, and preserve empty active/completed/failed jobs, no `fail_mission_bit_42`, and unchanged Federation reputation (`5`) under `sourceLabel=terminal-velocity-mission-deadline-abort-scaffold` / `oracleStatus=deadline_abort_pending_classic_runtime_or_manual_trace` for acceptance plus `sourceLabel=terminal-velocity-mission-abort-scaffold` / `oracleStatus=mission_abort_pending_classic_runtime_or_manual_trace` for the abort event. Symbolic scenario `mission_deadline_last_day_delivery_loop` now records the current Terminal Velocity deadline-boundary scaffold: accept the same timed dispatch, advance exactly to the limit day, deliver at Luna, and verify cargo release/reward with no failed job, no `fail_mission_bit_42`, and no reputation penalty under `sourceLabel=terminal-velocity-mission-deadline-last-day-scaffold` / `oracleStatus=deadline_last_day_delivery_pending_classic_runtime_or_manual_trace`. Godot now exposes the same boundary through `./run_godot.sh tv-mission-deadline-last-day-log` / `RunGodot.ps1 -MissionDeadlineLastDayLog`, logging `lastDayDeliveryCompleted=true`, `deadlineFailurePrevented=true`, `reservedCargoReleased=true`, `rewardPaid=true`, and `reputationPreserved=true` while keeping exact Classic date inclusivity/UI pending. Symbolic scenario `mission_deadline_completed_no_late_failure_loop` now verifies the completed-state guardrail: after delivery succeeds before expiry, advancing past the former deadline does not create a late failure, cargo leak, failure flag, or reputation penalty under `sourceLabel=terminal-velocity-mission-deadline-completed-no-late-failure-scaffold` / `oracleStatus=deadline_completed_no_late_failure_pending_classic_runtime_or_manual_trace`. Exact Classic `CanAbort` behavior, deadline inclusivity/date UI, completed-mission cleanup, and abort/failure interaction remain pending original-runtime/manual evidence.
  - Implementation addendum: symbolic scenario `mission_abort_forbidden_return_gate` now encodes the EV Classic Resource Bible `CanAbort` distinction as a guardrail: a probe mission with `canAbort=false` blocks `abort_active_mission`, preserves the active job and its 3 reserved cargo tons, and records `sourceLabel=ev-classic-resource-bible-backed-canabort-guardrail` / `oracleStatus=classic_runtime_canabort_ui_pending`. Source excerpt: `docs/references/ev-family/ev-classic-resource-bible.txt:462-470` says `CanAbort 0` means the mission cannot be aborted and must go to `ReturnStel` to become inactive, while nonzero missions can be aborted at any time and become inactive on failure. Exact runtime UI wording and return cleanup remain pending.
  - Implementation addendum: symbolic scenario `mission_abort_forbidden_return_completion_loop` now closes the CanAbort guardrail loop locally: after the blocked abort, the pilot travels to the destination/return body, completes `canabort_return_gate_probe`, releases the 3 reserved cargo tons, receives the scaffold reward, and records completion under `sourceLabel=ev-classic-resource-bible-backed-canabort-guardrail` / `oracleStatus=classic_runtime_canabort_return_cleanup_pending`. Exact Classic return-cleanup UI/status wording remains pending original runtime/manual evidence.
  - Implementation addendum: Godot now exposes the same CanAbort return-cleanup guardrail through `./run_godot.sh tv-mission-abort-forbidden-log` / `RunGodot.ps1 -MissionAbortForbiddenLog`: a `canAbort=false` active probe blocks abort, preserves the 3 reserved cargo tons, then completes at Luna, releases cargo, pays the reward, and logs `sourceLabel=ev-classic-resource-bible-backed-canabort-guardrail` / `oracleStatus=classic_runtime_canabort_return_cleanup_pending`. F10 help now also surfaces the player recovery rule for these resource-backed CanAbort return gates: stop retrying `X` and follow the Mission Log destination/return cleanup path; `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasCanAbortReturnHelp=true`.
  - Implementation addendum: symbolic scenario `mission_abort_reputation_penalty_guardrail` now models the EV Classic Resource Bible mission flag `0x0040 Apply -5x CompReward reversal on abort`: aborting `abort_penalty_probe` releases its 3 reserved cargo tons, records abort history, and applies a `-30` Federation reputation delta from `completionReward=6` and `abortReputationMultiplier=5` under `sourceLabel=ev-classic-resource-bible-backed-mission-abort-penalty-scaffold` / `oracleStatus=classic_runtime_abort_penalty_ui_pending`. Exact Classic abort UI wording and which stock missions carry the flag remain pending original-runtime/resource extraction evidence.
  - Implementation addendum: symbolic scenario `mission_auto_abort_completion_flags_guardrail` now models the EV Classic Resource Bible mission flag `0x0001 Marks the mission as an auto-aborting mission` and its completion-bit side effect: accepting `auto_abort_completion_bit_probe` immediately records `mission_auto_abort`, removes the active job, releases its 2 reserved cargo tons, records abort history, and applies `completionFlags=['auto_abort_completion_bit_77']` under `sourceLabel=ev-classic-resource-bible-backed-auto-abort-guardrail` / `oracleStatus=classic_runtime_auto_abort_ui_pending`. Exact Classic auto-abort UI/timing and stock mission mapping remain pending original-runtime/resource follow-up.
  - Implementation addendum: Godot now exposes the same auto-abort completion-bit guardrail through `./run_godot.sh tv-mission-auto-abort-log` / `RunGodot.ps1 -MissionAutoAbortLog`: a deterministic `auto_abort_completion_bit_probe` accepts with 2 reserved cargo tons, auto-aborts immediately, removes itself from active missions, releases reserved cargo, records abort history, applies `auto_abort_completion_bit_77`, and logs `sourceLabel=ev-classic-resource-bible-backed-auto-abort-guardrail` / `oracleStatus=classic_runtime_auto_abort_ui_pending`. Exact Classic auto-abort UI/status/timing remains pending original runtime/manual evidence.
  - Implementation addendum: Godot now exposes the same abort reputation-penalty guardrail through `./run_godot.sh tv-mission-abort-penalty-log` / `RunGodot.ps1 -MissionAbortPenaltyLog`: a deterministic `abort_penalty_probe` abort releases the 3 reserved cargo tons, applies `-(completionReward * abortReputationMultiplier)` to Federation reputation, records the delta in `aborted_mission_history`, and logs `sourceLabel=ev-classic-resource-bible-backed-mission-abort-penalty-scaffold` / `oracleStatus=classic_runtime_abort_penalty_ui_pending`. Exact Classic abort UI/status wording remains pending original runtime/manual evidence.
  - Implementation addendum: F10 help now surfaces abort reputation-penalty recovery for the existing Resource Bible-backed guardrail: after aborting a mission with reputation reversal, players should check Player Info abort history/legal standing, rebuild reputation, and re-scan Mission Computer gates before retrying. The same help slice also surfaces auto-abort recovery for Resource Bible-backed auto-abort probes: some accepted missions may remove themselves immediately and set completion flags, so players should verify Mission Log/Player Info history before assuming cargo remains active. A 2026-06-05 help addendum surfaces sequential-deadline recovery from the existing multi-failure guardrail: when multiple timed missions expire together, check Mission Log/Player Info failure history for each released-cargo job and reputation penalty before re-planning cargo or follow-up contracts. A timed-abort help addendum surfaces the existing deadline-abort no-late-failure guardrail: aborting a timed TV mission before expiry should release reserved cargo and prevent late failure flags/reputation penalties, so verify Mission Log/Player Info before accepting follow-ups. A last-day delivery help addendum surfaces the current deadline-boundary scaffold: timed TV missions can still complete on the scaffold deadline day, so verify reward/cargo/failure status in Mission Log/Player Info before waiting longer. A completed-deadline help addendum surfaces the existing no-late-failure guardrail: after delivering a timed TV mission, waiting past the former deadline should not create a late failure, so verify completed/rewarded Mission Log/Player Info state and no failure flag before re-planning. An active-deadline help addendum surfaces the existing active deadline display probe: before waiting or rerouting, check Mission Log/Player Info days remaining and complete timed deliveries before the TV deadline scaffold expires. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasMissionAbortPenaltyRecoveryHelp=true`, `hasMissionAutoAbortRecoveryHelp=true`, `hasMissionSequentialDeadlineRecoveryHelp=true`, `hasMissionTimedAbortRecoveryHelp=true`, `hasMissionLastDayDeliveryRecoveryHelp=true`, `hasMissionCompletedNoLateFailureHelp=true`, and `hasActiveDeadlineRecoveryHelp=true`; exact Classic abort/auto-abort/multi-failure/deadline-abort/date-inclusivity/completed-cleanup UI timing remains pending original-runtime evidence.
  - Implementation addendum: Godot now exposes the deadline-abort no-late-failure guardrail through `./run_godot.sh tv-mission-deadline-abort-log` / `RunGodot.ps1 -MissionDeadlineAbortLog`: deterministic timed mission `deadline_dispatch_failure_probe` aborts before expiry, releases its 3 reserved cargo tons, advances beyond its former deadline, prevents a late failure record/flag/reputation penalty, and logs `sourceLabel=terminal-velocity-mission-deadline-abort-scaffold` / `oracleStatus=deadline_abort_pending_classic_runtime_or_manual_trace`. Exact EV Classic abort/deadline interaction, UI wording, and deadline inclusivity remain pending original runtime/manual evidence.
  - Implementation addendum: Godot now exposes the mission deadline trade-carryover guardrail through `./run_godot.sh tv-mission-deadline-trade-carryover-log` / `RunGodot.ps1 -MissionDeadlineTradeCarryoverLog`: deterministic timed mission `deadline_dispatch_failure_probe` reserves 3 mission cargo tons, buys one Sol Food trade lot, misses the deadline, releases only mission cargo, preserves and sells the trade lot, records `fail_mission_bit_42`, and applies the half-`completionReward` Federation reputation penalty under `sourceLabel=terminal-velocity-mission-deadline-trade-carryover-scaffold` / `oracleStatus=deadline_failure_trade_carryover_pending_classic_runtime_or_manual_trace`. Exact EV Classic mixed mission/trade cargo cleanup UI and consequences remain pending original runtime/manual evidence.
  - Implementation addendum: symbolic scenario `mission_scan_failure_guardrail` now models the EV Classic Resource Bible `ScanGovt` / `FailIfScan` mission-field contract as a source-backed Terminal Velocity guardrail: a scan-sensitive dispatch survives a nonmatching Independent scan, then fails on a matching Federation scan, releases its 4 reserved cargo tons, records `fail_mission_bit_44`, and preserves `sourceLabel=ev-classic-resource-bible-backed-mission-scan-failure-scaffold` / `oracleStatus=classic_runtime_scan_failure_ui_pending`. Godot now exposes the same fast-eval lane through `./run_godot.sh tv-mission-scan-failure-log` / `RunGodot.ps1 -MissionScanFailureLog`, logging nonmatching scan preservation, matching scan failure, cargo release, failure flag, and latest failed-mission metadata. F10 help now surfaces the recovery path: scan-sensitive cargo missions can fail when the matching government scans, so players should check Mission Log/Player Info failure history, free released cargo, and seek follow-up contracts. Exact Classic scan frequency, cargo-scan UI wording, and stock mission mapping remain pending original-runtime/resource follow-up.
  - Verification addendum: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_abort_reputation_penalty_guardrail_applies_resource_bible_reversal -v`; `python3 tools/run_gameplay_scenarios.py mission_abort_reputation_penalty_guardrail --pretty`; broader native/Godot verification in the 2026-06-04 abort-penalty scenario slice. `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_scan_failure_guardrail_fails_only_on_matching_government_scan -v`; `python3 tools/run_gameplay_scenarios.py mission_scan_failure_guardrail --pretty`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_mission_scan_failure_log_contract -v`; `./run_godot.sh tv-mission-scan-failure-log`; full native discovery and Godot self-test passed in the 2026-06-04 scan-failure guardrail/Godot probe slices.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_alignment_completion_offer_scan_hides_closed_branch_offers -v`; `python3 tools/run_gameplay_scenarios.py alignment_completion_offer_scan_guardrail --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_alignment_story_prereq_recovery_blocks_and_restores_branch_offers -v`; `python3 tools/run_gameplay_scenarios.py alignment_story_prereq_recovery --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_alignment_offer_requirement_recovery_blocks_and_restores_branch_offers -v`; `python3 tools/run_gameplay_scenarios.py alignment_offer_requirement_recovery --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_abort_releases_reserved_cargo_without_completion -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_abort_reaccept_delivery_loop_recovers_after_abort -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_abort_forbidden_return_gate_preserves_active_job -v`; `python3 tools/run_gameplay_scenarios.py mission_abort_forbidden_return_gate --pretty`; `python3 tools/run_gameplay_scenarios.py mission_abort_reaccept_delivery_loop --pretty`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_deadline_failure_scaffold_releases_cargo_and_records_penalty -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_deadline_abort_prevents_failure_loop_releases_cargo_without_penalty -v`; `python3 tools/run_gameplay_scenarios.py mission_deadline_abort_prevents_failure_loop --pretty`; `python3 tools/run_gameplay_scenarios.py mission_deadline_trade_carryover_loop --pretty`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_mission_deadline_failure_log_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_mission_log_history_log_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_active_mission_deadline_log_contract -v`; `python3 -m unittest native_ev.tests.test_scenario_eval -v`; `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'`; `python3 tools/run_gameplay_scenarios.py --all --pretty`; `./run_godot.sh tv-mission-abort-log`; `./run_godot.sh tv-mission-deadline-failure-log`; `./run_godot.sh tv-mission-deadline-completed-log`; `./run_godot.sh tv-mission-deadline-abort-log`; `./run_godot.sh tv-mission-log-history-log`; `./run_godot.sh tv-active-mission-deadline-log`; `./run_godot.sh tv-mission-offer-scan-log`; `./run_godot.sh tv-mission-alignment-branch-log`; `./run_godot.sh self-test`.
  - Implementation addendum: Godot now exposes the alignment return-contract timing guardrail through `./run_godot.sh tv-mission-alignment-return-log` / `RunGodot.ps1 -MissionAlignmentReturnLog`: the deterministic log verifies `freeport_return_earth` is visible alongside both open chapter-one branch offers after `frontier_sample_hera_freeport`, remains as the only Mission Computer offer after the Federation branch is completed, and can be accepted/delivered after both Federation and Freeport branch completion states with the 5 reserved cargo tons released and the 3200-credit reward paid. The log keeps `sourceLabel=terminal-velocity-observed` / `oracleStatus=terminal_velocity_eval_pending_original_trace` plus `returnBoundary=terminal_velocity_return_contract_timing_scaffold_exact_classic_offer_ui_pending`, because exact EV Classic refresh timing/UI remains pending original-runtime evidence.
  - Next action: capture or mine original EV Classic/manual/resource evidence for exact mission abort/runtime failure UI, deadline date display, CanAbort behavior, failure descriptions, and special flags before changing player-facing wording or broader penalties.
  - Fidelity boundary: EVN fields inform structure; exact EV Classic field names/limits and abort/failure consequences require Classic confirmation.

- [ ] Map objective arrows, fuel/range, and hyperspace feedback
  - Status: `implemented Terminal Velocity scaffold / original mission-map auto-objective not observed`
  - Source: EV Nova Bible `Jump Distance`, fuel/nav interface fields, and mission map-arrow flags; local EV Classic hyperspace attempts remain `runtime-observed` but inconclusive. A 2026-05-28 original EV Classic/Basilisk capture with active missions visible in Mission Info found no visible automatic mission objective route/arrow/marker on the zoomed galaxy map; see `docs/research/original-ev-classic-runtime-observations.md` and local-only capture `C:\Games\BasiliskII\ev-mission-map-kathoon-active-missions-zoomout-20260528T235242Z.png`.
  - Scope: show mission travel/return/special-ship arrows, visible fuel/jump units, partial fuel state, route risk, and explicit feedback when jump conditions are not met.
  - Implementation: symbolic scenario `mission_destination_route_hint` accepts the intro courier mission and queues the active contract destination (`Centauri`) as the next route leg with `sourceLabel=terminal-velocity-design-scaffold` and `oracleStatus=mission_objective_hint_pending_ev_classic_ui_trace`; its route event now also carries the helper-facing objective hint plus `fuelRequired`, `fuelAvailable`, and `fuelWarning` so agents/UI can reason about route/fuel recovery without parsing prose. Symbolic scenario `mission_route_refuel_delivery_loop` extends the low-fuel route helper path through a blocked insufficient-fuel jump, landing/refueling at the named recovery body (`Earth`), consuming the queued route to `Centauri`, landing on `Luna`, and completing `intro_courier_earth_hera` under the same route-hint source/oracle boundary plus `terminal-velocity-mission-scaffold` mission behavior labels. Godot exposes the same route-hint surface through `./run_godot.sh tv-mission-route-hint-log` / `RunGodot.ps1 -MissionRouteHintLog`, emitting route hops, fuel before route, route fuel cost, pre-jump fuel-warning state, `routeStatusHasFuelHint=true`, `lowFuelRouteWarningVisible=true`, `lowFuelJumpBlocked=true`, `landedForRefuel=true`, `refuelSucceeded=true`, `deliveredAfterRefuel=true`, `completedMissions=["intro_courier_earth_hera"]`, and `staleRouteReplaced=true` when the `G` helper clears an unrelated queued route before queuing the active mission destination; in the player map, `G` queues the active mission route as a helper-labeled Terminal Velocity convenience and sets a visible status line with `Route fuel: N hop(s), cost N, fuel current/max`, appending `refuel before full route` when queued-route cost exceeds current fuel. Shift-click map routing shows the same fuel hint; `./run_godot.sh tv-map-route-log` covers that warning path.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_destination_route_hint_sets_route_to_active_contract_destination -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_mission_destination_route_hint_log_contract -v`; full native discovery passed 89 tests; Godot route/fuel logs passed: `tv-map-route-log`, `tv-route-jump-log`, `tv-low-fuel-jump-log`, `tv-route-land-refuel-log`, and `tv-mission-route-hint-log`. 2026-06-05 addendum: targeted TDD rerun of `test_mission_destination_route_hint_sets_route_to_active_contract_destination` verifies symbolic route events include structured fuel/objective-hint fields. 2026-06-05 addendum: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_mission_route_refuel_delivery_loop_recovers_and_delivers native_ev.tests.test_model.NativeEvModelTests.test_godot_mission_destination_route_hint_log_contract -v` passed 2/2; `python3 tools/run_gameplay_scenarios.py mission_route_refuel_delivery_loop --pretty` passed with all route/refuel/delivery checks; `./run_godot.sh tv-mission-route-hint-log` emitted `lowFuelJumpBlocked=true`, `landedForRefuel=true`, `refuelSucceeded=true`, `deliveredAfterRefuel=true`, and `completedMissions=["intro_courier_earth_hera"]`; `./run_godot.sh self-test` passed with `gameplayScenarios=62`. 2026-06-05 addendum: the Godot mission-route hint probe now also buys one 10-ton Food lot before queuing the mission route and verifies `tradeBoughtBeforeRoute=true`, `tradeCargoPreservedAfterDelivery=true`, `heldTradeCargoAfterDelivery=10`, and `cargoUsedAfterDelivery=10` under `missionTradeRouteSourceLabel=terminal-velocity-mission-trade-refuel-scaffold` / `missionTradeRouteOracleStatus=mission_trade_refuel_pending_classic_runtime_trace`, tying the visible G-helper/refuel/delivery loop to the existing mission/trade hybrid scaffold without claiming Classic auto-route or trade-carryover UI fidelity. 2026-06-05 addendum: the same Godot probe now sells the preserved Food lot at the Centauri/Luna destination market after mission delivery and verifies `tradeCargoSoldAfterDelivery=true`, `heldTradeCargoAfterSale=0`, `cargoUsedAfterSale=0`, and destination-sale credit increase, extending the route-helper probe through mission/trade cleanup under the same Terminal Velocity scaffold labels.
  - Implementation addendum: F10 help now promotes the existing navigation guardrail probe into player-facing recovery guidance for `J`/`L`/`F5` blocked states: no selected route, low fuel, too-close hyperspace, no nearby port, fast landing approach, and missing refuel service. `./run_godot.sh tv-gameplay-curriculum-help-log` reports `hasNavigationGuardrailRecoveryHelp=true`, and `./run_godot.sh tv-navigation-guardrail-log` continues to expose `noRouteGuidance=true`, `fuelGuidance=true`, `tooCloseGuidance=true`, `noPortGuidance=true`, `approachGuidance=true`, and `refuelGuidance=true` under `sourceLabel=terminal-velocity-navigation-guardrail-scaffold` / `oracleStatus=navigation_blocked_feedback_pending_playtest`.
  - 2026-06-06 implementation addendum: F10 help now promotes the existing mission-route/refuel probe into player-facing recovery guidance for stale `G` routes and low-fuel `J` blocks after queuing an active mission: keep the mission destination selected, land/refuel at a linked service port, then jump/deliver before buying more cargo. `./run_godot.sh tv-gameplay-curriculum-help-log` reports `hasMissionRouteRecoveryHelp=true`, and `./run_godot.sh tv-mission-route-hint-log` continues to expose `staleRouteReplaced=true`, `lowFuelJumpBlocked=true`, `landedForRefuel=true`, `refuelSucceeded=true`, and `deliveredAfterRefuel=true` under `sourceLabel=terminal-velocity-design-scaffold` / `oracleStatus=mission_objective_hint_pending_ev_classic_ui_trace` plus `missionTradeRouteSourceLabel=terminal-velocity-mission-trade-refuel-scaffold` / `missionTradeRouteOracleStatus=mission_trade_refuel_pending_classic_runtime_trace`.
  - Next action: keep the Terminal Velocity mission route hint, fuel precheck, and navigation blocked-reason recovery as opt-in/helper scaffolds, not EV Classic fidelity claims. To promote further, capture a stronger EV Classic pass that finds the mission destination on the map, tests multi-hop Shift-click routing through adjacent stops toward non-direct destinations such as Torgo Prime from Kathoon, and separately captures fuel/jump/land/refuel feedback.
  - Fidelity boundary: use EV-family docs for learning/UI structure; capture EV Classic runtime for exact jump timing/distance/sound/animation; do not claim original EV auto-draws active mission objective routes from the current capture.

- [ ] Per-government legal/reputation and AI behavior model
  - Status: `implemented Classic Resource Bible-backed legal/resource semantics scaffold / exact stock values and runtime/UI timing unconfirmed`
  - Source: local copies under `docs/references/ev-family/` plus field map `docs/research/ev-classic-resource-bible-legal-field-map.md`; decoded `gövt` semantics manifest `native_ev/data/sourced_ev_governments.json` generated by `tools/extract_ev_government_semantics.py` and decoded `mïsn` semantics manifest `native_ev/data/sourced_ev_missions.json` generated by `tools/extract_ev_mission_semantics.py`, both from `native_ev/data/sourced_ev_structures.json`. The EV Classic Resource Bible explicitly supports per-government legal record semantics (`gövt`), `CrimeTol`, `SmugPenalty`, disable/board/kill penalties, interceptor illegal-cargo scanning / piracy-police behavior, bribe/attack flags, ally/enemy propagation, mission legal availability (`AvailRecord`), mission scan legality (`ScanGovt` / `FailIfScanned`), mission government reward/failure reputation (`CompGovt` / `CompReward`), and mission legal-record cleaning via `PayVal`. Terminal Velocity now has decoded Classic government IDs/names/flags/CrimeTol/penalties and 116 decoded mission records through availability/travel/cargo/scan/pay/special-ship/completion fields; TV-specific government projection remains separate until the full stock-resource join replaces it.
  - Scope: track legal/reputation per government; drive landing rights, patrol aggression, bribes, mission eligibility, distress/greetings, roadside assistance, shipyard/outfitter access, and faction ally/enemy consequences.
  - Implementation: Godot now loads `native_ev/data/governments.json` and `native_ev/data/reputation.json`, persists `reputation_scores` and `legal_records` in TV pilot files, exposes current government/legal status on the HUD and Player Info overlay, provides `./run_godot.sh tv-legal-status-log` / `RunGodot.ps1 -LegalStatusLog` as a deterministic legal-threshold contract, and gates outfitter/shipyard purchase actions behind the same scaffold with a visible refusal message plus `./run_godot.sh tv-legal-service-gate-log` / `RunGodot.ps1 -LegalServiceGateLog`. The NPC scanner contacts and target-lock text now surface a Classic Resource Bible-backed CrimeTol-style legal patrol hostile posture; `./run_godot.sh tv-legal-patrol-posture-log` / `RunGodot.ps1 -LegalPatrolPostureLog` verifies the patrol warning without executing combat. Mission availability now honors `requirements.legalMin`, `requirements.legalMax`, and `requirements.reputationMin`, exposes visible blocked reasons on the Mission Computer, and `./run_godot.sh tv-mission-legal-eligibility-log` / `RunGodot.ps1 -MissionLegalEligibilityLog` verifies a clean legal contract becomes unavailable with a visible title-specific blocked reason and explicit source-boundary line under bad legal standing, matching the Classic `AvailRecord` resource concept without claiming exact Classic hidden/disabled UI wording. F10 help now surfaces mission legal eligibility recovery guidance for that existing probe: if Mission Computer hides or blocks a contract for legal standing, read the visible requirement reason, repair legal score or route to a qualifying government, then re-scan offers; `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasMissionLegalEligibilityRecoveryHelp=true`. With explicit approval to infer now/correct later, Godot also applies helper-labeled consequences: firing a final-hit projectile into a hostile legal patrol applies `destroy_patrol` legal/reputation deltas and a Terminal Velocity explosion scaffold (`tv-legal-consequence-log`) as a Classic govt penalty scaffold; landed `C` can buy reputation-gated clemency (`tv-legal-clemency-log`) as a still-inferred bribe/clearing placeholder; and landing with contraband applies a government fine/confiscation scan from `governments.json`/`reputation.json` (`tv-contraband-scan-log`) as a Classic smuggling-scan semantics scaffold with TV-tuned fines. The galaxy map selected-system details now include a legal-risk line for government/standing and contraband risk, and `./run_godot.sh tv-contraband-risk-log` verifies selected commodity contraband guidance before a mutating scan.
  - Implementation addendum: legal consequence scaffolds now expose the exact Terminal Velocity reputation/legal deltas applied from `destroy_patrol` in `./run_godot.sh tv-legal-consequence-log`: `sourceEvent=destroy_patrol`, `legalDeltaApplied`, `reputationDeltaApplied`, and `pirateReputationDeltaApplied`, while keeping `oracleStatus=classic_runtime_combat_resolution_pending` because exact Classic combat/legal resolution still needs original-runtime/source confirmation.
  - Implementation addendum: Player Info now surfaces a held-contraband risk line for the current government, including flagged commodity tons and fine-per-ton scaffold data; `./run_godot.sh tv-contraband-risk-log` reports `heldContrabandInventoryVisible=true` and `inventoryHint=...` under `sourceLabel=terminal-velocity-classic-resource-smuggling-risk-surface` / `oracleStatus=classic_runtime_scan_frequency_and_ui_wording_pending`. Symbolic scenario `contraband_scan_clemency_recovery` now extends that legal loop: held Federation contraband is confiscated, the government fine/legal delta is applied from data-backed enforcement semantics, and the player can pay a clemency scaffold to partially repair a bad legal record while exact Classic scan frequency/UI wording remains pending. Symbolic scenario `legal_docking_service_gate_recovery` now blocks low-legal Federation docking plus landed outfitter access, then verifies clemency restores both docking and an outfitter purchase under pending-Classic source boundaries. Symbolic scenario `weapon_reputation_gate_recovery` now blocks a Sirius Station `pulse_cannon` purchase below the Independent weapons-service reputation threshold, then raises reputation to the threshold and verifies the purchase under `terminal-velocity-weapon-reputation-gate-scaffold` / `classic_runtime_weapon_service_reputation_gate_pending` while preserving exact Classic service-refusal UI as pending. F10 help now surfaces contraband recovery guidance tied to the existing Player Info risk line: if cargo is illegal for the current government, sell or reroute before landing scans, or budget for clemency after confiscation; `./run_godot.sh tv-gameplay-curriculum-help-log` reports `hasContrabandRecoveryHelp=true`.
  - Implementation addendum: landing now honors the existing legal docking threshold scaffold before entering the landed panel. `./run_godot.sh tv-legal-docking-log` routes to Earth, sets a Fugitive Federation legal record, attempts to land, and reports `legalDockingDenied=true`, `landed=false`, and `patrolsHostile=true` under `sourceLabel=terminal-velocity-legal-docking-scaffold` / `oracleStatus=classic_runtime_docking_denial_ui_pending` so low-legal docking denial is player-visible and independently covered while exact Classic UI remains pending. F10 help now also explains the legal-docking denial recovery path (`check Player Info/legal messages and seek clemency or a safer port`) plus low-legal outfitter/shipyard service refusal recovery; `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLegalDockingHelp=true` and `hasLegalServiceHelp=true`.
  - Implementation addendum: the landed `C` clemency probe now exercises insufficient-credit blockage before successful recovery: `./run_godot.sh tv-legal-clemency-log` reports `clemencyInsufficientCreditBlocked=true`, `insufficientStatus="Clemency costs 1000 credits; insufficient funds"`, and `clemencyRecoveredAfterCredits=true` under `sourceLabel=terminal-velocity-inferred-clemency-scaffold` / `oracleStatus=approved_inference_pending_ev_classic_confirmation`. F10 help now surfaces this legal-clemency recovery path: if `C` is blocked, verify landed state, reputation threshold, low-enough legal standing, and credits before retrying. Exact Classic bribe/clemency UI remains pending.
  - Implementation addendum: F10 help now surfaces legal-consequence recovery for the existing `destroy_patrol` scaffold: after destroying a hostile patrol or government target, players should check Player Info legal/reputation deltas, route to a safer port, and use clemency only when eligible. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLegalConsequenceRecoveryHelp=true`, and `./run_godot.sh tv-legal-consequence-log` continues to expose `sourceEvent=destroy_patrol`, `legalDeltaApplied`, `reputationDeltaApplied`, `pirateReputationDeltaApplied`, `sourceLabel=terminal-velocity-classic-resource-govt-penalty-semantics`, and `oracleStatus=classic_runtime_combat_resolution_pending`.
  - Implementation addendum: F10 help now also surfaces legal-patrol posture recovery for the existing CrimeTol-style patrol scaffold: if scanner or target status marks patrols hostile, players should treat their legal standing as below the current TV tolerance scaffold, route to a safer government or repair legal standing before fighting, and keep exact Classic patrol AI/UI pending. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLegalPatrolPostureRecoveryHelp=true`, and `./run_godot.sh tv-legal-patrol-posture-log` exposes `patrolWarning=true`, `hostilePosture=true`, `targetStatus="Closest target: Contact 1 hostile legal patrol..."`, `sourceLabel=terminal-velocity-classic-resource-patrol-semantics`, and `oracleStatus=classic_runtime_combat_timing_pending`.
  - Implementation addendum: Godot landed purchases now route weapon buys through `serviceRequirements.weapons.reputationMinByGovernment`, so Sirius Station `pulse_cannon` purchase attempts visibly block below the Independent reputation threshold and succeed once reputation meets it. `./run_godot.sh tv-weapon-reputation-gate-log` / `RunGodot.ps1 -WeaponReputationGateLog` reports `weaponReputationBlocked=true`, `weaponBoughtAfterReputation=true`, and `sourceLabel=terminal-velocity-weapon-reputation-gate-scaffold` while keeping exact Classic weapon-service refusal UI pending.
  - 2026-06-06 implementation addendum: symbolic scenario `weapon_credit_recovery_loop` now covers the adjacent affordability edge for the same Sirius Station `pulse_cannon`: one credit short of the 1,400-credit Terminal Velocity price blocks without purchase, then exact-price recovery buys the weapon and leaves credits at 0. Events use `sourceLabel=terminal-velocity-weapon-credit-gate-scaffold` / `oracleStatus=classic_runtime_weapon_purchase_credit_ui_pending`; this is a Terminal Velocity purchase guardrail scaffold until Classic weapon-purchase refusal wording/timing is runtime-confirmed.
  - 2026-06-06 weapon credit recovery help addendum: F10 help now promotes the existing `weapon_credit_recovery_loop` outcome into player-facing recovery guidance: if a landed weapon purchase is blocked for insufficient credits, run a safe trade/mission funding leg, return to the same service port, and retry only once the price is covered. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasWeaponCreditRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-weapon-credit-gate-scaffold` / `oracleStatus=classic_runtime_weapon_purchase_credit_ui_pending`.
  - 2026-06-06 weapon availability addendum: symbolic scenario `weapon_availability_recovery_loop` covers the adjacent service-inventory edge for `pulse_cannon`: a purchase attempt at Sol/Earth blocks because the local Terminal Velocity manifest does not sell that weapon, then the route recovers at Sirius/Sirius Station and buys it under `sourceLabel=terminal-velocity-weapon-availability-scaffold` / `oracleStatus=classic_runtime_weapon_store_inventory_pending`. This is a TV weapon-store availability scaffold until exact Classic outfitter/weapon inventory UI is runtime-confirmed.
  - 2026-06-06 weapon availability recovery help addendum: F10 help now promotes the existing `weapon_availability_recovery_loop` outcome into player-facing recovery guidance: if a landed weapon is not sold at the current port, keep the purchase goal, scout the service matrix for a port that lists it, then retry there. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasWeaponAvailabilityRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-weapon-availability-scaffold` / `oracleStatus=classic_runtime_weapon_store_inventory_pending`.
  - 2026-06-06 weapon purchase mission-cargo addendum: symbolic scenario `weapon_purchase_mission_cargo_reservation_loop` verifies an active 3-ton delivery at Sirius Station keeps its reserved cargo space when the player buys a `pulse_cannon`; the purchase records `activeMissionCargo=3`, leaves `cargoUsed=3`, and ends with 8,600 credits under `sourceLabel=terminal-velocity-weapon-mission-cargo-scaffold` / `oracleStatus=classic_runtime_weapon_purchase_cargo_interaction_pending`. This is a Terminal Velocity weapon/cargo interaction scaffold pending exact Classic purchase and mission-cargo UI/runtime evidence.
  - 2026-06-06 weapon mission-cargo recovery help addendum: F10 help now promotes the existing `weapon_purchase_mission_cargo_reservation_loop` source boundary into player-facing guidance: after buying a landed weapon while carrying active mission cargo, verify Mission Log/Player Info reserved cargo before rerouting or accepting more freight. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasWeaponMissionCargoRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-weapon-mission-cargo-scaffold` / `oracleStatus=classic_runtime_weapon_purchase_cargo_interaction_pending` for the underlying scenario.
  - 2026-06-06 weapon purchase trade-cargo addendum: symbolic scenario `weapon_purchase_trade_cargo_reservation_loop` verifies a held 10-ton food lot at Sirius Station stays in the cargo hold when the player buys a `pulse_cannon`; the purchase trace records `cargoHold={"food": 10}`, `cargoUsed=10`, `activeMissionCargo=0`, and ends with 7,860 credits under `sourceLabel=terminal-velocity-weapon-trade-cargo-scaffold` / `oracleStatus=classic_runtime_weapon_purchase_cargo_interaction_pending`. This is a Terminal Velocity weapon/trade-cargo interaction scaffold pending exact Classic purchase and cargo UI/runtime evidence.
  - 2026-06-06 weapon trade-cargo recovery help addendum: F10 help now promotes the existing `weapon_purchase_trade_cargo_reservation_loop` source boundary into player-facing guidance: after buying a landed weapon while holding trade cargo, verify Player Info/commodity hold before selling, rerouting, or accepting more freight. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasWeaponTradeCargoRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-weapon-trade-cargo-scaffold` / `oracleStatus=classic_runtime_weapon_purchase_cargo_interaction_pending` for the underlying scenario.
  - 2026-06-06 weapon legal-docking recovery addendum: symbolic scenario `weapon_legal_docking_recovery_loop` now blocks Federation docking at Earth at legal `-70`, pays the existing clemency scaffold to recover to `-45`, lands, jumps to Sirius Station, and buys a `pulse_cannon` with credits ending at `0`. The docking block uses `sourceLabel=terminal-velocity-legal-docking-scaffold` / `oracleStatus=classic_runtime_docking_denial_ui_pending`; the weapon purchase uses `sourceLabel=terminal-velocity-weapon-legal-docking-scaffold` / `oracleStatus=classic_runtime_weapon_purchase_after_docking_denial_pending`, so this remains a Terminal Velocity legal/weapon recovery scaffold pending exact Classic docking/refusal and weapon-purchase UI evidence.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_weapon_purchase_mission_cargo_reservation_loop_preserves_active_job_space native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_weapon_purchase_trade_cargo_reservation_loop_preserves_held_lot native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_weapon_legal_docking_recovery_loop_repairs_legal_record_then_buys_weapon -v`; `python3 tools/run_gameplay_scenarios.py weapon_purchase_mission_cargo_reservation_loop --pretty`; `python3 tools/run_gameplay_scenarios.py weapon_purchase_trade_cargo_reservation_loop --pretty`; `python3 tools/run_gameplay_scenarios.py weapon_legal_docking_recovery_loop --pretty`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `./run_godot.sh tv-gameplay-curriculum-help-log`.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_sourced_ev_governments_manifest_maps_classic_resource_bible_fields native_ev.tests.test_model.NativeEvModelTests.test_sourced_ev_missions_manifest_maps_classic_resource_bible_fields -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_legal_status_surface_is_scaffold_contract native_ev.tests.test_model.NativeEvModelTests.test_godot_outfitter_shipyard_purchases_feed_recent_messages_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `python3 -m unittest native_ev.tests.test_model -v`; `python3 tools/extract_ev_government_semantics.py`; `python3 tools/extract_ev_mission_semantics.py`; `./run_godot.sh tv-legal-status-log`; `./run_godot.sh tv-legal-docking-log`; `./run_godot.sh tv-gameplay-curriculum-help-log`; `./run_godot.sh tv-legal-service-gate-log`; `./run_godot.sh tv-weapon-reputation-gate-log`; `./run_godot.sh tv-legal-patrol-posture-log`; `./run_godot.sh tv-mission-legal-eligibility-log`; `./run_godot.sh tv-legal-consequence-log`; `./run_godot.sh tv-legal-clemency-log`; `./run_godot.sh tv-contraband-scan-log`; `./run_godot.sh tv-contraband-risk-log`; `./run_godot.sh tv-map-route-log`; `./run_godot.sh self-test`.
  - Next action: compare inferred mechanics against EV Classic runtime and EV/Override/Nova plugin-manual/Bible fields such as government crime tolerance, scan fines, contraband, bribery, and mission legal availability; tune labels/thresholds from evidence without blocking current playable scaffolds.
  - Fidelity boundary: formulas, exact thresholds, and original UI wording need EV Classic confirmation before being promoted from TV scaffold to Classic fidelity claim.

- [x] Cargo/trade coupling and mission-reserved capacity
  - Status: `implemented Terminal Velocity scaffold / source-family structured`
  - Source: EVN mission cargo fields, Nova Bible commodity purchase-location fields, and disaster/commodity-price references.
  - Scope: make cargo a shared constraint across missions and trade; expose mission-reserved cargo/passengers in trade screens; retain discovered buy/sell markets, route profit per jump/day, fuel/refuel risk, and event/disaster price effects.
  - Implementation: Godot Mission Info (`I`) summarizes active mission count, reserved mission tons, and free capacity; the HUD shows `Cargo: used/capacity (mission, free)`; Commodity Exchange displays `Cargo reserved for missions`, explicit `Buy B` and `Sell S` columns, buy/sell prices, and only marks sell action on held cargo. The selected commodity now shows a Terminal Velocity scaffold hint for the best linked-system sell price/profit per ton when local market data exists. Mission acceptance and commodity buys use the shared `_cargo_available_tons()` helper so mission cargo and trade cargo compete visibly for the same hold. A small HUD `Messages:` stack now preserves recent mission/trade success and blocked-reason feedback such as insufficient free cargo, full hold, unavailable commodity, no sell price, no cargo to sell, or insufficient credits.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_commodity_buy_sell_affordance_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_commodity_route_hint_contract -v`; `./run_godot.sh tv-commodity-trade-log` emitted `buySucceeded=true sellSucceeded=true roundTripVisible=true`; full native discovery passed 96 tests; Godot self-test passed with `gameplayScenarios=12`.
  - Next action: defer exact Classic commodity prices/events until decoded resources or runtime confirmation; use this surface for future commodity-market edge cases after the route/fuel/hyperspace feedback slice is stronger.
  - Fidelity boundary: exact Classic commodity prices/events require decoded resources or runtime confirmation.

- [x] Ship/outfit comparison and upgrade consequence surfaces
  - Status: `implemented Terminal Velocity scaffold / source-backed manifest fields`
  - Source: local EV Classic ship-like records in `native_ev/data/ships.json`, local shipyard PICT assets, local outfitter effect manifest; EV Nova Bible outfit fields remain source-family guidance only.
  - Scope: expose purchase deltas for mass, cargo/outfit space, crew/capture effects, weapon roles, licenses/permits, resale restrictions, and persistence across hull changes.
  - Implementation: Shipyard listings now show immediate deltas against the current hull for cargo, hull, max speed, and turning; outfitter listings now show concise numeric effect summaries for cargo, hull, and fuel upgrades. Buying hull/fuel outfits applies the advertised max-hull/max-fuel effects. Existing Source PICT shipyard art remains visible on the selected listing. Godot also has deterministic `tv-outfitter-shipyard-log` and `tv-repair-service-log` scenarios: the repair log first verifies in-space `F7` repair attempts are blocked with actionable landing guidance, then routes to Sol/Earth, damages hull, applies the manifest `pricePerHullPoint` repair cost, restores hull, verifies already-full repair feedback, and verifies insufficient-credit repair attempts preserve credits/hull with a visible `Not enough credits` message under `sourceLabel=terminal-velocity-repair-service-scaffold`. Symbolic scenario `repair_insufficient_credit_guardrail` now mirrors that credit edge: one credit short of the calculated repair cost blocks while preserving hull damage and credits, then exact-cost recovery repairs the ship under pending-Classic labels. Symbolic scenario `outfitter_purchase_guardrail_recovery_loop` blocks outfit/ship purchases while in space, at Levo where the outfit is not for sale, and without credits, then recovers by buying a cargo pod and light freighter under `sourceLabel=terminal-velocity-outfitter-purchase-guardrail-scaffold`. Symbolic scenario `shipyard_overfull_cargo_guardrail` and the Godot `tv-outfitter-shipyard-log` now also block downsizing into a smaller ship while current cargo exceeds the target hold, preserve the overfull cargo before recovery, and allow the purchase after freeing cargo under `cargoGuardrailSourceLabel=terminal-velocity-shipyard-cargo-guardrail-scaffold` / `cargoGuardrailOracleStatus=shipyard_cargo_transfer_pending_ev_classic_runtime_trace`. F10 gameplay help now explicitly points players at the shipyard/outfitter delta/effects previews, and `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasShipyardDeltaHelp=true`.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_landing_panels_are_actionable -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_outfitter_shipyard_progression_log_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_repair_service_log_contract -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_repair_insufficient_credit_guardrail_preserves_damage_then_recovers -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_outfitter_purchase_guardrail_recovery_loop_blocks_then_buys -v`; `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_shipyard_overfull_cargo_guardrail_blocks_downsize_until_cargo_fits -v`; `python3 tools/run_gameplay_scenarios.py repair_insufficient_credit_guardrail --pretty`; `python3 tools/run_gameplay_scenarios.py shipyard_overfull_cargo_guardrail --pretty`; `./run_godot.sh self-test`; `./run_godot.sh tv-outfitter-shipyard-log`; `./run_godot.sh tv-repair-service-log`.
  - Next action: gather original-runtime outfitter/shipyard purchase traces on a disposable non-strict pilot before claiming Classic fidelity for purchase semantics, item stats, or exact persistence behavior.
  - Fidelity boundary: exact EV Classic item stats and persistence behavior require Classic resource/manual/runtime confirmation; current comparison uses the local structured manifest as a Terminal Velocity scaffold.

- [x] In-game learning/documentation surfaces for EV-style strategy
  - Status: `implemented Terminal Velocity helper/scaffold`
  - Source: same EV-family deep dive plus recurring guide/player-pain signals from GameFAQs, EVN Wiki, Ambrosia/Cythera archives, and EV Nova manual/walkthroughs.
  - Scope: add or plan durable player learning aids: beginner guide, keyboard overlay, map/service reference, trading guide, combat guide, ship/outfit comparison help, faction/storyline hinting, and mission checklist/log.
  - Implementation: Godot now has an F10 help overlay that explains currently wired keys, map route planning, the `G` mission-route helper, route/fuel hop-cost and refuel-before-full-route status warnings before jumps, Backspace/Delete route clearing plus fresh route re-selection before retrying `J`, active-mission destination markers on the galaxy map, mission/free cargo reservation, mission offer detail/gate/branch-choice helper lines, mission deadline countdown/failure scaffold guidance, mission abort/history preservation guidance for completed/aborted/failed jobs, mission-route trade-sale recovery guidance after preserved cargo reaches the destination market, first mission delivery recovery guidance for keeping route/mission-log state aligned through the intro courier delivery before taking chain offers, landing tabs, buy/accept controls, recent message feedback, blocked-action recovery guidance for failed jumps/buys/landing/fire/repair/mission actions, player-info/inventory overlay, map service summaries for selected systems, shipyard/outfitter comparison surfaces, shipyard cargo-transfer recovery guidance for blocked trade-downs, weapon reputation gate recovery guidance for reputation-blocked local weapon purchases, refuel, F7 hull repair where available, combat targeting/fire controls (`Tab`, `N`, `R`) plus selected-target scanner/HUD details with exact Classic targeting UI pending, pirate-avoidance route/refuel guidance that explicitly prefers escaping to a linked safe port before fighting when an intercept warning appears, combat-salvage recovery guidance for green in-space salvage markers plus full-hold persistence visibility on HUD/Player Info, shield-recharge/combat-readiness guidance pointing players to Player Info while retaining decoded-resource-backed scaffold labels, contraband-risk guidance that points players to map commodity hints and Player Info before mutating scan/fine checks, F8 disabled-player recovery as a Terminal Velocity scaffold pending Classic death/reload evidence, and F6 pilot save/resume. The overlay explicitly labels itself `Terminal Velocity helper/scaffold — not an EV Classic fidelity claim`; `./run_godot.sh tv-gameplay-curriculum-help-log` exposes booleans such as `hasPirateAvoidanceHelp`, `hasMissionRouteTradeSaleHelp`, `hasFirstMissionDeliveryRecoveryHelp`, `hasServiceProvisioningHelp`, `hasStartingEquipmentUncertaintyHelp`, and `hasTravelTimingRecoveryHelp` for deterministic regression.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_landing_panels_are_actionable -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_player_inventory_overlay_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_map_service_summary_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_map_mission_destination_marker_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_help_exposes_gameplay_curriculum_hints_contract -v`; `./run_godot.sh tv-gameplay-curriculum-help-log` now verifies `hasFirstMissionDeliveryRecoveryHelp=true`, `hasMissionRouteTradeSaleHelp=true`, `hasShieldRechargeHelp=true`, `hasWeaponReputationHelp=true`, `hasMissionAbortHistoryHelp=true`, `hasMissionOfferHelp=true`, `hasMissionObjectiveHelp=true`, `hasMissionRouteHelp=true`, `hasPilotPersistenceHelp=true`, `hasPlayerInfoHelp=true`, `hasRecentMessagesHelp=true`, `hasBlockedActionHelp=true`, and `hasRouteClearHelp=true` along with the other help-surface flags.
  - Next action: broad Classic strategy/tutorial detail should wait for stronger manual/runtime evidence; keep promoting scenario outcomes into helper-labeled UI only when playtest feedback shows a confusing loop.
  - Fidelity boundary: learning aids may explain Terminal Velocity behavior directly; do not present unverified EV Classic facts as canonical.

- [x] Visible mission log/status detail
  - Status: `implemented Terminal Velocity helper/scaffold`
  - Source: Terminal Velocity mission JSON and recurring EV-family player-guide pattern that active contracts need visible destination/cargo/reward status; exact EV Classic mission log window layout/text remains unverified.
  - Scope: make active mission state visible without relying on transient status text: title, active status, destination system/body, current progress, route hint, cargo reserved, reward, briefing when available, and recent completion history with cargo/reward settlement.
  - Implementation: `I` now toggles a Mission Log overlay and still updates the status line summary; the overlay is labeled `Terminal Velocity mission log helper/scaffold — not an EV Classic fidelity claim`; active contracts show whether to travel, land, or complete at the current port plus a `G` route-helper hint; completed missions now record recent title/port/cargo-released/reward-paid history and persist it in the TV pilot JSON. `P` Player Info now also includes hull/max-hull with the current repair cost, source-labeled primary weapon summary, original-runtime-observed `No Secondary Weapon` starting-HUD boundary text, active mission title/destination, deadline remaining, cargo/reward, compact mission-history counts plus latest failed-mission reputation delta, and `terminal-velocity-player-info-mission-scaffold` / `terminal-velocity-mission-log-history-scaffold` source-boundary text so mission-running, repair, combat equipment, and post-failure recovery status are visible outside the dedicated mission log.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_player_inventory_overlay_contract -v`; `./run_godot.sh self-test`; prior mission log contract verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_mission_completion_history_contract -v`; full native discovery passed 94 tests; Godot self-test passed with `gameplayScenarios=12`. Player Info history addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_mission_log_history_log_contract -v`; `./run_godot.sh tv-mission-log-history-log` emitted `playerInfoHistoryVisible=true` and `playerInfoFailureVisible=true`.
  - Next action: mission-log screenshots only after stronger UI evidence or a visual-review need appears; otherwise keep this as a regression surface while route/fuel/hyperspace feedback remains the next likely non-gated playable slice.
  - Fidelity boundary: this is TV usability scaffolding, not a claim about the original Classic mission-log UI.

- [x] Pilot save/resume persistence affordance
  - Status: `implemented Terminal Velocity save scaffold / needs EV Classic pilot-file evidence`
  - Source: Terminal Velocity JSON pilot persistence path and existing title-screen New/Open Pilot flow; exact EV Classic pilot file layout and autosave/manual-save semantics remain unverified.
  - Scope: make longer sessions resumable by saving current system, position/velocity/facing, fuel, credits, cargo/cargo space, active/completed missions, story flags, commodity hold, owned outfits/weapons, ship identity, recent status feedback, and Strict Play flag.
  - Implementation: in-flight `F6` now saves the currently loaded pilot through the same `user://pilots/*.tvpilot.json` path used by New/Open Pilot; the HUD and F10 help expose `F6 save`; the title-screen Open Pilot list shows pilot/ship, saved system, credits, Strict Play status, active mission destination summary, and most recent saved status before resume; outfitter and shipyard purchase outcomes feed the same recent-message history as mission/trade actions; `./run_godot.sh tv-pilot-save-resume-log` / `RunGodot.ps1 -PilotSaveResumeLog` creates/overwrites a deterministic non-strict test pilot, accepts a courier mission, buys a ship plus outfit/weapon inventory, saves, mutates state including inventory/ship/cargo-space, selected secondary weapon index, recent status feedback, and the Strict Play flag, reopens the pilot, and checks round-trip system/fuel/credits/active-mission/Strict-Play/outfit/weapon/selected-secondary/ship/cargo-space/status fields.
  - Verification: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_pilot_save_resume_log_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_open_pilot_list_shows_resume_context_contract -v`; `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_outfitter_shipyard_purchases_feed_recent_messages_contract -v`; `./run_godot.sh tv-pilot-save-resume-log` emitted `saveSucceeded=true`, `resumeSucceeded=true`, `systemRoundTrip=true`, `fuelRoundTrip=true`, `creditsRoundTrip=true`, `missionRoundTrip=true`, `strictPlayRoundTrip=true`, `outfitRoundTrip=true`, `weaponRoundTrip=true`, `selectedSecondaryRoundTrip=true`, `shipRoundTrip=true`, `cargoSpaceRoundTrip=true`, and `statusRoundTrip=true`; full native discovery passed 107 tests; Godot self-test passed with `gameplayScenarios=14`.
  - Next action: defer exact Classic pilot-file fidelity until original pilot-file/resource evidence exists; keep save/resume in regression coverage while moving to route/fuel/hyperspace feedback or original-runtime evidence capture.
  - Fidelity boundary: this is Terminal Velocity save/persistence implementation, not an EV Classic pilot-file fidelity claim.

- [x] Automated gameplay learning harness and skill curriculum
  - Status: `implemented-extended`
  - Source: `docs/research/ev-automated-gameplay-learning-synthesis.md`; `docs/research/ev-gameplay-autoresearch-results.jsonl`; source-grounded EV-family mission data from `/tmp/ev-source-deep-dive-20260521/text/`; Tea Leaves early EV merchant-loop guide; LP Archive EV Nova role/playstyle framing; prior Basilisk stabilization notes.
  - Scope: build a symbolic/LLM-controller gameplay loop for Terminal Velocity before attempting long unattended Basilisk play. Minimum pieces: structured game-state observer, bounded action API, reusable skill library, scenario/evaluator ladder, action/evidence trace logging, and separate non-strict pilot/scenario profiles for merchant, route-planner, mission-runner, outfitter, and disposable combat.
  - Initial curriculum: screen classifier; Levo commodity 10-ton lot; safe passenger/cargo mission; route to known neighbor and land/refuel; mission completion; opportunistic trade with mission-reserved cargo; mission-surface archive; map objective arrows; pirate avoidance; blocked-reason recognition for combat/cargo/legal/ship gates; disabled-player recovery.
  - Implementation: `native_ev/scenario_eval.py` provides a structured state/action/evaluator harness and the scenario curriculum listed in `native_ev/data/gameplay_curriculum.json`. Current scaffold lanes include merchant/mission delivery, mission-offer scanning, story-chain and alignment guardrails, Federation branch delivery, mission route hints, mission/trade capacity planning, mission abort/reaccept recovery and deadline failure, outfitter/ship-ladder planning, shipyard overfull-cargo downsizing guardrails, repair-service recovery, disabled-player recovery, system service provisioning plus the Godot `tv-service-provisioning-log` probe, route queue guardrails, near-center/low-fuel/refuel blockers, pirate avoidance plus pirate low-fuel recovery help/log fields, blocked-action recognition, contraband scan/legal clemency recovery, legal clemency insufficient-credit guardrails, legal docking/service gate recovery, weapon-service reputation gate recovery, and disposable-combat guardrails. The `federation_alignment_delivery_loop` lane accepts the Federation Sirius report branch, delivers it to Earth, preserves `alignment_federation` / `federation_intel_asset`, and verifies the incompatible Freeport branch remains blocked under `terminal-velocity-mission-scaffold` / `mission_behavior_pending_classic_runtime_trace`. The `shipyard_overfull_cargo_guardrail` lane blocks downsizing from a light freighter to shuttlecraft while current cargo exceeds the smaller hold, preserves the cargo before recovery, then succeeds after freeing cargo while labeling the lane `terminal-velocity-shipyard-cargo-guardrail-scaffold` / `shipyard_cargo_transfer_pending_ev_classic_runtime_trace`. The `repair_service_recovery_loop` lane damages hull, verifies repair blocks in space and at Levo's no-repair port, then repairs at Sol/Earth with the manifest `pricePerHullPoint` while labeling the lane `terminal-velocity-repair-service-scaffold` / `repair_service_pending_ev_classic_runtime_trace`. The `disabled_player_recovery_loop` lane forces the player ship disabled, verifies jump/fire/mission actions are blocked, then recovers through the Terminal Velocity F8 scaffold with `terminal-velocity-player-disabled-scaffold` / `classic_runtime_player_death_pending_strict_play_safe_trace` labels. The `system_service_provisioning_scout` lane records Levo's original-runtime-observed absent outfitter/shipyard boundary, then scouts Earth and Stardock Alpha service/store matrices as Terminal Velocity provisioning scaffolds pending Classic confirmation.
  - Verification: `python3 -m unittest native_ev.tests.test_scenario_eval -v`; `python3 tools/run_gameplay_scenarios.py weapon_reputation_gate_recovery --pretty`; `python3 tools/run_gameplay_scenarios.py --all` (56/56 passed); `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'` (177 tests passed); Godot `./run_godot.sh self-test` passed with `gameplayScenarios=56`; prior probes include `./run_godot.sh tv-pirate-avoidance-log`, `./run_godot.sh tv-mission-abort-log`, `./run_godot.sh tv-outfitter-shipyard-log`, `./run_godot.sh tv-gameplay-curriculum-help-log`, and `./run_godot.sh tv-combat-guardrail-log`. 2026-06-06 service-provisioning Godot addendum: `python3 -m unittest native_ev.tests.test_model.NativeEvModelTests.test_godot_service_provisioning_log_contract -v` passed and `./run_godot.sh tv-service-provisioning-log` emitted `levoNoOutfitterObserved=true`, `earthFullServiceScaffold=true`, `stardockNoShipyardScaffold=true`, and `serviceMatrixScoutVisible=true` under `sourceLabel=original-runtime-observed-levo-plus-terminal-velocity-service-provisioning-scaffold` / `oracleStatus=classic_runtime_service_matrix_pending`. 2026-06-06 pirate low-fuel recovery addendum: help contract and `tv-pirate-avoidance-log` now expose `hasPirateLowFuelRecoveryHelp`, `lowFuelEscapeBlocked=true`, and `refueledBeforeEscape=true` while keeping `sourceLabel=terminal-velocity-pirate-avoidance-scaffold` / `oracleStatus=pirate_avoidance_pending_ev_classic_combat_trace`; verified with targeted native help/pirate contracts and `./run_godot.sh tv-pirate-avoidance-log`.
  - 2026-06-06 hull-plating repair addendum: symbolic scenario `hull_plating_repair_loop` now buys `hull_plating` at Sol/Earth, verifies max hull increases from 100 to 125 while current hull remains repairable at 100, repairs the added 25 hull capacity for the manifest cost of 25 × 8 = 200 credits, and records explicit `terminal-velocity-hull-plating-repair-scaffold` / `hull_plating_repair_pending_classic_runtime_trace` checkpoints alongside the existing repair-service scaffold labels.
  - 2026-06-06 hull-plating repair verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_hull_plating_repair_loop_buys_armor_and_repairs_added_hull -v`; `python3 -m unittest native_ev.tests.test_scenario_eval -v` (73 tests); `python3 tools/run_gameplay_scenarios.py hull_plating_repair_loop --pretty`; `python3 tools/run_gameplay_scenarios.py --all` (69/69 passed); `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'` (203 tests).
  - 2026-06-06 balanced upgrade trade addendum: symbolic scenario `balanced_upgrade_trade_loop` now starts from a constrained 3,600-credit Earth refit budget, buys `cargo_pod` and `fuel_tank`, verifies `hull_plating` is blocked at 900 credits, runs two Sol Food lots back to Levo to fund the final hull refit, returns to Earth, buys `hull_plating`, and repairs the added 25 hull capacity. Events use `sourceLabel=terminal-velocity-balanced-upgrade-trade-scaffold` / `oracleStatus=balanced_upgrade_budget_pending_classic_runtime_trace` plus the existing repair-service scaffold labels; this remains a Terminal Velocity scaffold pending Classic runtime confirmation.
  - 2026-06-06 balanced upgrade trade verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_balanced_upgrade_trade_loop_funds_final_refit_with_trade_run`; `python3 tools/run_gameplay_scenarios.py balanced_upgrade_trade_loop --pretty` (success true, final credits 460, cargo/fuel/hull upgrades installed, hull 125/125).
  - 2026-06-06 light freighter capacity trade addendum: symbolic scenario `light_freighter_capacity_trade_loop` now buys a `light_freighter` at Sol/Earth from a bounded high-credit training state, verifies ship cargo capacity expands to 150, carries six Sol Food lots back to Levo, sells the expanded hold load, and records `ship_capacity_upgrade` / `large_hold_trade_run` checkpoints. Events use `sourceLabel=terminal-velocity-light-freighter-trade-scaffold` / `oracleStatus=light_freighter_trade_pending_classic_runtime_trace`; this remains a Terminal Velocity scaffold until Classic ship purchase, capacity transfer, and trade-run behavior are runtime/resource confirmed.
  - 2026-06-06 light freighter capacity trade verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_light_freighter_capacity_trade_loop_uses_ship_capacity_for_large_return_load -v`; `python3 tools/run_gameplay_scenarios.py light_freighter_capacity_trade_loop --pretty` (success true, final credits 10858, ship `light_freighter`, cargo capacity 150, cargo cleared).
  - 2026-06-06 light freighter mission/trade addendum: symbolic scenario `light_freighter_mission_trade_loop` now buys a `light_freighter` at Sol/Earth, reserves an 80-ton Levo delivery, fills the remaining hold with six Sol Food lots for a 140-ton mixed mission/trade load, completes the delivery before selling the retained trade cargo, and records `bulk_mission_capacity` / `mission_trade_cargo_recovery` checkpoints. Events use `sourceLabel=terminal-velocity-light-freighter-mission-trade-scaffold` / `oracleStatus=light_freighter_mission_trade_pending_classic_runtime_trace`; this remains a Terminal Velocity scaffold until Classic mission cargo reservation, Light Freighter capacity, and mixed mission/trade sale ordering are runtime/resource confirmed.
  - 2026-06-06 light freighter mission/trade verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_available_scenarios_exposes_curriculum_order native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_light_freighter_mission_trade_loop_reserves_mission_cargo_alongside_bulk_trade -v`; `python3 -m unittest native_ev.tests.test_scenario_eval -v` (76 tests); `python3 tools/run_gameplay_scenarios.py light_freighter_mission_trade_loop --pretty` (success true, final credits 16880, ship `light_freighter`, cargo capacity 150, completed `levo_bulk_freighter_supply`, cargo cleared).
  - 2026-06-06 Light Freighter mission/trade capacity help addendum: F10 help now promotes the existing `light_freighter_mission_trade_loop` outcome into player-facing recovery guidance: after upgrading to a Light Freighter, reserve bulk mission cargo first, fill only remaining hold with profitable trade cargo, deliver the job, then sell retained cargo before accepting another bulk contract. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLightFreighterMissionTradeCapacityHelp=true` while retaining `sourceLabel=terminal-velocity-light-freighter-mission-trade-scaffold` / `oracleStatus=light_freighter_mission_trade_pending_classic_runtime_trace`.
  - 2026-06-06 light freighter refuel delivery addendum: symbolic scenario `light_freighter_refuel_delivery_loop` now buys a `light_freighter` at Sol/Earth, reserves a 120-ton Levo bulk delivery, forces a zero-fuel return gap, verifies the loaded jump blocks with `insufficient fuel`, refuels at Earth to the Light Freighter's 300-unit reserve, then completes `levo_bulk_refuel_supply` at Levo with cargo released. It records `bulk_delivery_refuel_gap` / `refueled_bulk_delivery_recovery` checkpoints using `sourceLabel=terminal-velocity-light-freighter-refuel-delivery-scaffold` / `oracleStatus=light_freighter_refuel_delivery_pending_classic_runtime_trace`; this remains a Terminal Velocity scaffold pending Classic runtime/resource confirmation of Light Freighter fuel/cargo and mission/refuel ordering.
  - 2026-06-06 light freighter refuel delivery verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_available_scenarios_exposes_curriculum_order native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_light_freighter_refuel_delivery_loop_recovers_low_fuel_bulk_delivery -v`; `python3 -m unittest native_ev.tests.test_scenario_eval -v` (77 tests); `python3 tools/run_gameplay_scenarios.py light_freighter_refuel_delivery_loop --pretty` (success true, final credits 12200, ship `light_freighter`, max fuel 300, final fuel 299, completed `levo_bulk_refuel_supply`, cargo cleared); `python3 tools/run_gameplay_scenarios.py --all --pretty` (73/73 passed); `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'` (207 tests); `./run_godot.sh self-test` (gameplayScenarios=73); `git diff --check`. `npm test` is not applicable at repo root because no `package.json` exists there.
  - 2026-06-06 light freighter bulk mission margin addendum: symbolic scenario `light_freighter_bulk_mission_margin_loop` reserves a 120-ton Levo delivery after buying a Light Freighter, evaluates remaining hold space for profitable Sol Food versus negative Levo Equipment, fills only the free 30 tons with Food, delivers the bulk mission, then sells the retained trade cargo under explicit pending-Classic source boundaries.
  - 2026-06-06 light freighter bulk mission margin verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_available_scenarios_exposes_curriculum_order native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_light_freighter_bulk_mission_margin_loop_fills_remaining_hold_after_reservation -v`; `python3 tools/run_gameplay_scenarios.py light_freighter_bulk_mission_margin_loop --pretty` (success true, final credits 19,540, final cargo empty, completed `levo_bulk_margin_supply`).
  - 2026-06-06 light freighter refuel mission margin addendum: symbolic scenario `light_freighter_refuel_mission_margin_loop` combines the 120-ton Light Freighter bulk delivery, remaining-hold margin choice, and zero-fuel recovery: reserve `levo_bulk_refuel_margin_supply`, choose three profitable Sol Food lots while skipping negative-margin Equipment, block the loaded return jump on insufficient fuel, refuel at Earth, then complete delivery and sell retained trade cargo under `terminal-velocity-light-freighter-refuel-mission-margin-scaffold` / `light_freighter_refuel_mission_margin_pending_classic_runtime_trace` boundaries.
  - 2026-06-06 light freighter refuel mission margin verification: `python -m pytest native_ev/tests/test_scenario_eval.py -k light_freighter_refuel_mission_margin_loop -q` (1 passed); `python3 tools/run_gameplay_scenarios.py light_freighter_refuel_mission_margin_loop --pretty` (success true, final credits 19,540, final cargo empty, fuel 299, completed `levo_bulk_refuel_margin_supply`); `python -m pytest native_ev/tests/test_scenario_eval.py -q` (80 passed); `python3 tools/run_gameplay_scenarios.py --all --pretty` (76/76 passed); `python3 -m unittest discover -s native_ev/tests -p 'test_*.py'` (210 tests); `./run_godot.sh self-test` (GODOT SELFTEST OK, gameplayScenarios=76); `git diff --check`. `npm test` is not applicable at repo root because no `package.json` exists there.
  - 2026-06-06 Light Freighter refuel margin help addendum: F10 help now promotes the existing `light_freighter_refuel_mission_margin_loop` outcome into player-facing recovery guidance: after reserving a bulk delivery, fill only the remaining hold with positive-margin cargo, preserve a service-port refuel plan, then deliver and sell retained cargo. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLightFreighterRefuelMarginRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-light-freighter-refuel-mission-margin-scaffold` / `oracleStatus=light_freighter_refuel_mission_margin_pending_classic_runtime_trace`.
  - 2026-06-06 light freighter repair mission margin addendum: symbolic scenario `light_freighter_repair_mission_margin_loop` combines damaged-hull repair budgeting with a 120-ton bulk delivery and remaining-hold margin choice, then repairs at Earth after mission pay plus retained trade cargo fund the service bill. Verification: `python3 -m unittest native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_available_scenarios_exposes_curriculum_order native_ev.tests.test_scenario_eval.ScenarioEvalHarnessTests.test_light_freighter_repair_mission_margin_loop_repairs_after_bulk_delivery_and_margin_sale -v`; `python3 tools/run_gameplay_scenarios.py light_freighter_repair_mission_margin_loop --pretty` (success true, final credits 9302, final hull 300/300, final cargo empty, completed `levo_bulk_repair_margin_supply`). Source labels: `terminal-velocity-light-freighter-repair-mission-margin-scaffold` / `light_freighter_repair_mission_margin_pending_classic_runtime_trace`; repair boundary: `terminal-velocity-repair-service-scaffold` / `repair_service_pending_ev_classic_runtime_trace`.
  - 2026-06-06 Light Freighter repair mission margin help addendum: F10 help now promotes the existing `light_freighter_repair_mission_margin_loop` outcome into player-facing recovery guidance: when a damaged freighter carries a bulk delivery, reserve repair credits, fill only spare hold with positive-margin cargo, deliver, sell retained cargo, then return to a repair-service port before taking another risky job. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLightFreighterRepairMissionMarginRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-light-freighter-repair-mission-margin-scaffold` / `oracleStatus=light_freighter_repair_mission_margin_pending_classic_runtime_trace` plus the repair-service boundary.
  - 2026-06-06 disposable combat evidence gate help addendum: F10 help now promotes the existing `disposable_combat_placeholder` / pirate-combat safety boundary into player-facing guidance before any Classic-runtime pirate/combat trace: use a disposable non-Strict pilot, verify controls/save state, avoid reusable pilots, and stop on unsafe hull/shield conditions. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasDisposableCombatEvidenceGateHelp=true`; this is a Terminal Velocity guardrail/help surface, not Classic combat behavior evidence.
  - 2026-06-06 Light Freighter repair/refuel margin help addendum: F10 help now promotes the existing `light_freighter_repair_refuel_mission_margin_loop` outcome into player-facing recovery guidance: when a damaged, low-fuel freighter carries a bulk delivery, reserve repair credits, buy only positive-margin spare cargo, deliver and sell at Levo, refuel before the empty return, then repair at Earth. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLightFreighterRepairRefuelMarginRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-light-freighter-repair-refuel-mission-margin-scaffold` / `oracleStatus=light_freighter_repair_refuel_mission_margin_pending_classic_runtime_trace` plus the repair-service boundary.
  - 2026-06-06 Light Freighter deadline repair/refuel margin help addendum: F10 help now promotes the existing `light_freighter_deadline_repair_refuel_margin_loop` outcome into player-facing recovery guidance: deliver the timed bulk job on the final allowed day before waiting or returning for repairs, then sell retained cargo, refuel at the service port, and repair at Earth. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasLightFreighterDeadlineRepairRefuelMarginRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-light-freighter-deadline-repair-refuel-margin-scaffold` / `oracleStatus=light_freighter_deadline_repair_refuel_margin_pending_classic_runtime_trace` plus the repair-service boundary.
  - 2026-06-06 contraband scan/trade recovery addendum: symbolic scenario `contraband_scan_trade_recovery_loop` now starts with a mixed Earth hold (`equipment` plus legal `food`), verifies the Federation scan confiscates only contraband, pays clemency, then jumps to Levo and sells the preserved Food lot. Events use `sourceLabel=terminal-velocity-contraband-trade-recovery-scaffold` / `oracleStatus=classic_runtime_scan_trade_cargo_cleanup_pending`; this remains a Terminal Velocity scaffold until Classic runtime scan/cargo cleanup and UI behavior are traced.
  - 2026-06-06 contraband scan/trade recovery help addendum: F10 help now promotes the existing `contraband_scan_trade_recovery_loop` outcome into player-facing recovery guidance: after a scan removes illegal cargo from a mixed hold, check preserved legal cargo in Player Info, pay clemency only when needed, then sell the legal lot at a safe market. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasContrabandTradeRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-contraband-trade-recovery-scaffold` / `oracleStatus=classic_runtime_scan_trade_cargo_cleanup_pending` for the underlying scenario.
  - 2026-06-06 weapon inventory stack recovery help addendum: F10 help now promotes the existing `weapon_inventory_stack_recovery_loop` outcome into player-facing recovery guidance: repeated secondary purchases should stack in owned weapons instead of replacing the first copy, so players should verify Player Info/secondary selection before buying another. `./run_godot.sh tv-gameplay-curriculum-help-log` verifies `hasWeaponInventoryStackRecoveryHelp=true` while retaining `sourceLabel=terminal-velocity-weapon-inventory-stack-scaffold` / `oracleStatus=classic_runtime_multiple_weapon_purchase_inventory_pending` for the underlying scenario.
  - Next action: gather original-runtime evidence for pirate avoidance and combat on disposable non-strict pilots only after controls and save safety are understood; continue promoting scenario outcomes into visible tutorial/help surfaces as playtest feedback identifies confusing loops.
  - Fidelity boundary: use this as an automation/design scaffold. Original EV Classic fidelity claims still require original runtime, decoded Classic resources, or manual/docs confirmation; long unattended Basilisk play, Strict Play, and piracy/combat on reusable pilots remain gated.


## Deferred / guardrail items

- [ ] External adaptation comparisons
  - Status: `deferred`
  - Source: external engines/adaptations may provide hypotheses only.
  - Rule: do not implement behavior from external adaptations unless original runtime, decoded resources, manual/docs, or Terminal Velocity instrumentation confirms it.
